# --- Secrets / local env (never commit real values) ---
.env
.env.*
!.env.example
# Optional gitignored local Spring overrides (if you ever add one)
application-local.yaml
application-local.yml
application-local.properties

# --- mkcert certs (operator-provided, never commit) ---
frontend/certs/
backend/src/main/resources/certs/
*.p12
*.pem

# --- OS / editor ---
.DS_Store
Thumbs.db
.planning/research/.cache

# --- GSD local config (per-machine, not shared) ---
.planning/config.json