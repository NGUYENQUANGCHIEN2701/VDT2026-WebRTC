# Cấu hình coturn (STUN + TURN) cho VDT WebRTC.

listening-port=3478
fingerprint
use-auth-secret
realm=vdt-webrtc.local

# Dải port relay NHỎ (Docker không map nổi 16k port )
min-port=49160
max-port=49200

# Log ra stdout để xem bằng `docker compose logs coturn`
log-file=stdout
verbose
