---
phase: 09-monitoring-ci-cd-full-delivery
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - docs/setup.md
  - .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [INFR-02, INFR-04, INFR-05, INFR-06]
must_haves:
  truths:
    - "docker compose up --build brings up all 9 services (postgres, backend-1, backend-2, nginx+frontend, redis, rabbitmq, coturn, prometheus, grafana) and every one reports healthy in docker compose ps"
    - "A human opens http://localhost:8080, places a real 1-1 call in two browser tabs, and confirms video/audio works exactly as before this phase's changes (no regression to the P2P call core)"
    - "A human opens Grafana at http://localhost:3000, sees the VDT WebRTC Overview dashboard auto-provisioned, and watches WS-sessions-per-instance and active-calls panels move while placing a test call"
    - "docs/setup.md accurately describes the full docker compose up flow including frontend, Prometheus, and Grafana — the stale 'Frontend chưa nằm trong Compose' note is removed"
    - "09-VALIDATION.md's Per-Task Verification Map is filled in with real Plan/Wave/Task IDs and all rows are green"
  artifacts:
    - .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md (updated, nyquist_compliant: true)
  key_links:
    - "docs/setup.md docker compose up section -> reflects the actual 9-service stack from Plan 09-02"
    - "ROADMAP.md Phase 9 entry -> marked complete with final plan/wave counts, matching the pattern used for Phases 6-8's closing plans"
---

<objective>
Run the full verification gate for Phase 9: automated backend/frontend/E2E suites green, a manual `docker compose up` walkthrough confirming all 9 services healthy and the Grafana dashboard renders live per-instance data during a real call, update `docs/setup.md` to describe the now-complete stack, and close out `09-VALIDATION.md`/`ROADMAP.md`.

Purpose: Phases 6, 7, and 8 all closed with a dedicated "full suite gate + manual checkpoint" plan before being marked complete in ROADMAP.md — this plan follows that same established pattern for Phase 9, the final phase of the v1 milestone. It also catches integration issues between Plans 09-01 through 09-04 that isolated per-plan verification cannot (e.g., does the dashboard actually render once Prometheus is truly scraping both instances under real call load; does the E2E job's assumption about TURN-free ICE actually hold on a real CI run).
Output: Full backend + frontend + Playwright suites green; `docker compose up --build` demo confirmed healthy end-to-end; `docs/setup.md` updated; `09-VALIDATION.md` closed out; `ROADMAP.md` Phase 9 marked complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-monitoring-ci-cd-full-delivery/09-CONTEXT.md
@.planning/phases/09-monitoring-ci-cd-full-delivery/09-RESEARCH.md
@.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md
@docs/setup.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full automated suite gate</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md (Full suite command row: mvn verify + vitest run + playwright test; Sampling Rate section's "Before /gsd-verify-work" bullet)
  </read_first>
  <action>
    Run the complete backend suite: `cd backend && ./mvnw -B verify` (must include the Phase 6 cross-instance Redis Testcontainers test and the new `CallMetricsTest` from Plan 09-01, all green). Run the complete frontend suite: `cd frontend && npm ci && npm run lint && npm run test:run && npm run build` (all green, build artifact produced). Run the Playwright E2E suite against a locally-started backend+frontend (per Plan 09-04's Task 2 approach — no full docker compose required for this local run): confirm `npm run e2e` passes. Record the pass/fail state of each of these 3 suites; any failure must be fixed (re-invoking the relevant Plan 09-01 through 09-04 task's acceptance criteria) before proceeding to Task 2.
  </action>
  <acceptance_criteria>
    - `cd backend && ./mvnw -B verify` exits 0
    - `cd frontend && npm run lint && npm run test:run && npm run build` all exit 0
    - `cd frontend && npm run e2e` exits 0 against a locally-started backend+frontend
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ./mvnw -B verify</automated>
  </verify>
  <done>Backend mvn verify, frontend lint/test/build, and Playwright E2E all pass locally before the manual compose checkpoint.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update docs/setup.md for the full 9-service stack</name>
  <files>docs/setup.md</files>
  <read_first>
    docs/setup.md (current "Cách 1 — Chạy full-stack bằng Docker Compose" section, lines 29-40 — contains a one-line note left over from before this phase, saying the frontend was not yet part of the Compose stack and would be added in a later phase; this phase IS that later phase, so the note is now stale and must be replaced)
  </read_first>
  <action>
    In `docs/setup.md`, update the "Cách 1 — Chạy full-stack bằng Docker Compose" section: delete the leftover one-line note about the frontend not yet being part of Compose (it predates Plan 09-02's changes and is no longer accurate), and instead list the complete service set that `docker compose up --build` now starts: postgres, backend-1, backend-2, nginx (serving the built frontend at `http://localhost:8080` and proxying `/api`+`/ws`), redis, rabbitmq, coturn, prometheus (`http://localhost:9090`), grafana (`http://localhost:3000`, default login `admin` / value of `GRAFANA_ADMIN_PASSWORD`). Add a short new subsection documenting the Grafana/Prometheus URLs and the "VDT WebRTC Overview" dashboard name, matching this doc's existing Vietnamese-language, table-driven style (see the "Cấu hình biến môi trường" table for formatting convention). Add `GRAFANA_ADMIN_PASSWORD` to the existing env-var table (the one listing `POSTGRES_DB`/`JWT_SECRET`/etc.).
  </action>
  <acceptance_criteria>
    - `docs/setup.md` no longer contains any note stating the frontend is absent from the Compose stack (see the `<verify>` grep below for the exact string being checked)
    - `docs/setup.md` mentions `localhost:9090` (Prometheus) and `localhost:3000` (Grafana)
    - `docs/setup.md`'s env-var table includes `GRAFANA_ADMIN_PASSWORD`
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'Frontend chưa nằm trong Compose' docs/setup.md)" -eq 0 && grep -q 'localhost:9090' docs/setup.md && grep -q 'localhost:3000' docs/setup.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>docs/setup.md accurately describes the complete 9-service docker compose stack including frontend, Prometheus, and Grafana access points, with no stale prior-phase notes remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: Manual docker compose + Grafana + CI checkpoint</name>
  <files>
    (no production changes)
  </files>
  <action>
    What was built: Micrometer-backed per-instance metrics (Plan 09-01), the full 9-service `docker compose up` stack with frontend+Prometheus+Grafana folded in (Plan 09-02), a 4-job GitHub Actions CI pipeline including a real 2-context Playwright call test (Plans 09-03/09-04), and updated setup docs (this plan's Task 2).

    How to verify — execute all 5 steps and record pass/fail for each:
    1. From the repo root, copy `.env.example` to `.env` if not already done, then run `docker compose up --build`. Wait for all services to report healthy: `docker compose ps` should show `postgres`, `backend-1`, `backend-2`, `nginx`, `redis`, `rabbitmq`, `coturn`, `prometheus`, `grafana` all as `healthy` (or `running` for services without a healthcheck).
    2. Open `http://localhost:8080` in two browser tabs/profiles, log in as two different users (or register two new ones), and place a 1-1 video call exactly as in prior phases — confirm video/audio still works with no regression (this proves the nginx `location /` SPA change and the frontend containerization did not break the existing call flow).
    3. Open `http://localhost:9090/targets` (Prometheus) — confirm both `backend-1:8080` and `backend-2:8080` show as `UP` under the `vdt-backend` job.
    4. Open `http://localhost:3000` (Grafana), log in with `admin` / your configured `GRAFANA_ADMIN_PASSWORD` (or `admin` if left default for local demo), and confirm the "VDT WebRTC Overview" dashboard is already there with no manual setup. While the 1-1 call from step 2 is active, confirm the "WS Sessions per Instance" and "Active Calls by Type" panels show non-zero, per-instance-distinguishable data.
    5. Push this phase's branch (or open a PR) and confirm all 4 GitHub Actions jobs (`backend`, `frontend`, `docker-build`, `e2e`) go green — pay particular attention to the `e2e` job, since 09-RESEARCH.md flagged the "two same-host Chromium contexts negotiate ICE without TURN" assumption (A3) as the single highest-uncertainty claim in this phase; if it fails, the documented fallback is adding a public STUN server to the E2E build's ICE config via a `VITE_ICE_SERVERS` override — investigate and apply if needed.

    Resume signal: type "approved" once all 5 verification steps pass, or describe which step failed and its symptoms.
  </action>
  <done>All 5 manual verification steps pass: compose stack fully healthy, 1-1 call regression-free, Prometheus shows both instances UP, Grafana dashboard renders live per-instance data, and all 4 CI jobs are green on a real push.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Close out 09-VALIDATION.md and mark Phase 9 complete in ROADMAP.md</name>
  <files>.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md (Per-Task Verification Map table with placeholder `TBD-01`..`TBD-04` rows and `nyquist_compliant: false` in frontmatter)
    .planning/ROADMAP.md (Phase 9 section — "Plans: TBD" line and the Progress table's "9. Monitoring, CI/CD & Full Delivery | 0/TBD | Not started" row; compare against how Phase 8's entry reads once complete, for the target format)
  </read_first>
  <action>
    In `.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md`, replace the 4 `TBD-*` placeholder rows in the "Per-Task Verification Map" table with the real Plan/Wave/Task identifiers from Plans 09-01 through 09-05 (e.g. `09-03/T2` for INFR-05, `09-04/T2-T3` for INFR-06, `09-02/T1-T2` for INFR-02, `09-01`+`09-02/T3` for INFR-04), set every row's Status column to `✅ green` once Task 1's automated suites and the human-verify checkpoint above both pass, set frontmatter `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, and fill in the "Approval" line at the bottom with the date this checkpoint was approved.
    In `.planning/ROADMAP.md`, update the Phase 9 section: change `**Plans**: TBD` to `**Plans:** 5 plans` and add the plan list (`- [ ] 09-01-PLAN.md — ...` through `09-05-PLAN.md — ...`, one line per plan with a brief objective, mirroring the format used in Phase 1-8 sections), mark the phase's checkbox `[x]` in the "Phases" list at the top of the file, and update the Progress table row for Phase 9 to `5/5` with a `✅ Complete` status and today's date, matching the exact formatting of the Phase 8 row directly above it.
  </action>
  <acceptance_criteria>
    - `.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md` frontmatter has `status: verified` and `nyquist_compliant: true`
    - No `TBD-01`/`TBD-02`/`TBD-03`/`TBD-04` strings remain in the file
    - `.planning/ROADMAP.md` Phase 9 checkbox is `[x]` in the phases list
    - `.planning/ROADMAP.md` Progress table shows `9. Monitoring, CI/CD & Full Delivery | 5/5 | ✅ Complete | <date>`
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'TBD-0' .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md)" -eq 0 && grep -q 'nyquist_compliant: true' .planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>09-VALIDATION.md is fully closed out with real IDs and green status; ROADMAP.md reflects Phase 9 as complete with 5/5 plans, matching the established closure pattern from Phases 6-8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan verifies boundaries established in Plans 09-01 through 09-04 (actuator exposure, Grafana credentials, CI secret hygiene) rather than introducing new ones.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Manual verification step 3 (Prometheus targets page) | low | accept | Confirming `/actuator/prometheus` is scraped correctly is itself part of proving T-09-01's mitigation (from Plan 09-01) actually holds end-to-end — this checkpoint step exists specifically to catch a regression, not introduce a new exposure |

</threat_model>

<verification>
- `cd backend && ./mvnw -B verify` green
- `cd frontend && npm run lint && npm run test:run && npm run build` green
- `cd frontend && npm run e2e` green (local run)
- Human-verify checkpoint approved (5 steps: compose healthy, 1-1 call regression check, Prometheus targets UP, Grafana dashboard live data, CI 4 jobs green)
- `.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md` and `.planning/ROADMAP.md` updated
</verification>

<success_criteria>
- All automated suites (backend, frontend, E2E) green
- Full `docker compose up --build` demo confirmed healthy with a real working 1-1 call and a live Grafana dashboard
- CI green on a real push including the E2E job
- Phase 9 marked complete in ROADMAP.md; 09-VALIDATION.md closed out with real IDs
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `docs/setup.md` — updated to describe the full 9-service compose stack, Prometheus/Grafana URLs, `GRAFANA_ADMIN_PASSWORD` env var
- `.planning/phases/09-monitoring-ci-cd-full-delivery/09-VALIDATION.md` — closed out (`status: verified`, `nyquist_compliant: true`, real Task IDs, all rows green)
- `.planning/ROADMAP.md` — Phase 9 marked complete, 5/5 plans, Progress table updated

<output>
Create `.planning/phases/09-monitoring-ci-cd-full-delivery/09-05-SUMMARY.md` when done
</output>
