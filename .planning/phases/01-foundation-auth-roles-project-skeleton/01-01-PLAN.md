---
phase: 01-foundation-auth-roles-project-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/.mvn/wrapper/maven-wrapper.properties
  - backend/src/main/resources/application.yaml
  - backend/src/main/resources/application-docker.yaml
  - backend/src/main/resources/db/migration/V1__create_tables.sql
  - backend/src/main/resources/db/migration/V2__seed_admin.sql
  - backend/Dockerfile
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/src/index.css
  - frontend/Dockerfile
  - frontend/nginx.conf
  - docker-compose.yml
  - docker-compose.dev.yml
  - backend/src/test/java/com/vdt/webrtc/auth/AuthControllerTest.java
  - backend/src/test/java/com/vdt/webrtc/config/SecurityConfigTest.java
  - backend/src/test/java/com/vdt/webrtc/FlywayMigrationTest.java
  - frontend/vitest.config.ts
  - frontend/src/api/axiosInstance.test.ts
  - frontend/src/auth/AuthContext.test.tsx
  - frontend/src/auth/ProtectedRoute.test.tsx
autonomous: true
requirements:
  - INFR-07

must_haves:
  truths:
    - "D-05: Monorepo layout — `backend/` (Spring Boot) and `frontend/` (React+Vite) live in the same repo, scaffolded by this plan"
    - "Running `docker compose up` starts backend, frontend (nginx), and postgres without errors"
    - "Flyway applies V1__create_tables.sql and V2__seed_admin.sql on backend startup; `flyway_schema_history` has exactly 2 entries"
    - "Backend health endpoint responds: `curl http://localhost:8080/actuator/health` returns HTTP 200 with `{status:UP}`"
    - "Frontend dev server starts via `cd frontend && npm run dev`; React app loads in browser without console errors"
    - "All backend test stub files compile and run (all tests pass trivially — they are stubs for Wave 0)"
    - "All frontend test stub files run without errors under Vitest"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Spring Boot 4.0.x BOM, JJWT 0.13.0, Flyway, Testcontainers"
      contains: "spring-boot-starter-parent 4.0."
    - path: "backend/src/main/resources/db/migration/V1__create_tables.sql"
      provides: "users and refresh_tokens schema"
      contains: "CREATE TABLE users"
    - path: "backend/src/main/resources/db/migration/V2__seed_admin.sql"
      provides: "Admin seed record"
      contains: "INSERT INTO users"
    - path: "docker-compose.yml"
      provides: "Full-stack Compose"
      contains: "condition: service_healthy"
    - path: "frontend/package.json"
      provides: "Frontend dependencies"
      contains: "react-router"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest jsdom config"
      contains: "jsdom"
  key_links:
    - from: "docker-compose.yml backend service"
      to: "postgres service"
      via: "depends_on condition: service_healthy"
      pattern: "service_healthy"
    - from: "application-docker.yaml"
      to: "postgres Compose service"
      via: "jdbc:postgresql://postgres:5432/vdt_webrtc"
      pattern: "postgres:5432"
---

<objective>
Set up the monorepo project scaffold, locking all stack versions, establishing the Flyway migration baseline, and creating Wave 0 test stubs. After this plan the repo compiles, the DB schema exists, Docker Compose starts the full stack, and all test files exist (stubs) so later plans have a green baseline to build on.

Purpose: A skeleton that compiles and starts is the foundation every subsequent plan builds on. No user-visible features yet — this is the "can the stack run?" proof.
Output: Compilable backend, installable frontend, Flyway V1+V2 migrations, full-stack docker-compose.yml, Wave 0 test stubs (all trivially passing).
</objective>

<execution_context>
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-PATTERNS.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-RESEARCH.md
</execution_context>

<context>
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/ROADMAP.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-CONTEXT.md
@D:/VDTWebRTC/VDT2026-WebRTC/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend scaffold — pom.xml, application configs, Flyway migrations, Dockerfile</name>
  <read_first>
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-PATTERNS.md (sections: "backend/pom.xml", "application.yml", "application-docker.yml", "V1__create_tables.sql", "V2__seed_admin.sql", "backend/Dockerfile")
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-RESEARCH.md (sections: Standard Stack, Pattern 3, Pattern 7, Pattern 8, Pitfall 5, Pitfall 6, Pitfall 9, Pitfall 10, Environment Availability)
  </read_first>
  <action>
    Generate the Spring Boot project at `backend/` using Maven wrapper. Use Spring Initializr conventions (no web UI needed — create files directly).

    `backend/pom.xml`: parent `org.springframework.boot:spring-boot-starter-parent:4.0.x` — use the latest available 4.0.x patch (e.g. 4.0.7 — do NOT use 4.1.0; CLAUDE.md locks Boot to 4.0.x per D-locked stack). Group `com.vdt`, artifact `webrtc`, Java 21. Dependencies (all versions via Boot BOM unless noted):
    - `spring-boot-starter-webmvc` (Boot 4 renamed `-web` → `-webmvc`; pom ALREADY uses this)
    - `spring-boot-starter-security`
    - `spring-boot-starter-data-jpa`
    - `spring-boot-starter-validation`
    - `spring-boot-starter-actuator`
    - `spring-boot-starter-test` (test scope)
    - `org.flywaydb:flyway-core` (no version — BOM)
    - `org.flywaydb:flyway-database-postgresql` (no version — BOM)
    - `org.postgresql:postgresql` (runtime, no version — BOM)
    - `io.jsonwebtoken:jjwt-api:0.13.0`
    - `io.jsonwebtoken:jjwt-impl:0.13.0` (runtime)
    - `io.jsonwebtoken:jjwt-jackson:0.13.0` (runtime)
    - `org.springdoc:springdoc-openapi-starter-webmvc-ui:3.0.3` — CURRENTLY OMITTED from pom (optional; was left out during initial scaffold). Add only if Swagger UI is wanted; if `./mvnw compile` fails on it, leave it out and note in `docs/setup.md`. Do NOT let this optional dependency block the plan.
    - `org.springframework.boot:spring-boot-testcontainers` (test scope)
    - `org.testcontainers:postgresql:1.21.0` (test scope)
    - `org.testcontainers:junit-jupiter:1.21.0` (test scope)
    Set `<java.version>21</java.version>` in properties.

    `backend/.mvn/wrapper/maven-wrapper.properties`: set `distributionUrl` to Maven 3.9.9 (or latest 3.9.x stable). Also create `mvnw` and `mvnw.cmd` wrapper scripts (standard Spring Initializr content).

    `backend/src/main/resources/application.yaml` (ALREADY EXISTS — credentials externalized to env vars per quick task 260613-kxw; do NOT revert to hardcoded values):
    - `spring.datasource.url: ${DB_URL:jdbc:postgresql://localhost:5432/vdt_webrtc}`, username: `${DB_USERNAME:vdt}`, password: `${DB_PASSWORD:vdt_pass}`
    - `spring.jpa.hibernate.ddl-auto: validate` (NEVER `create` or `update` — Flyway owns schema)
    - `spring.jpa.show-sql: false`
    - `spring.flyway.enabled: true`, `locations: classpath:db/migration`, `baseline-on-migrate: false`
    - `jwt.secret: ${JWT_SECRET:dev-only-secret-...}` (dev default for local; real value via .env in Docker), `jwt.access-token-ttl-ms: 900000`
    - `server.port: 8080`
    - `management.endpoints.web.exposure.include: health`

    `backend/src/main/resources/application-docker.yaml` (ALREADY EXISTS): overrides datasource URL to `jdbc:postgresql://postgres:5432/vdt_webrtc` (the Compose service name). NOTE: the existing docker-compose.yml currently injects `DB_URL` via env directly, so the `docker` profile is optional — keep this file as the documented Docker override either way.

    `backend/src/main/resources/db/migration/V1__create_tables.sql`: create `users` table (id BIGSERIAL PK, username VARCHAR(50) NOT NULL UNIQUE, email VARCHAR(255) NOT NULL UNIQUE, password_hash VARCHAR(100) NOT NULL, role VARCHAR(20) NOT NULL DEFAULT 'USER', locked BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()) and `refresh_tokens` table (id BIGSERIAL PK, user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE, token_hash VARCHAR(64) NOT NULL UNIQUE, expires_at TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), revoked BOOLEAN NOT NULL DEFAULT FALSE). Create indexes `idx_refresh_tokens_user_id` on refresh_tokens(user_id) and `idx_refresh_tokens_token_hash` on refresh_tokens(token_hash). Double underscore in filename is MANDATORY — single underscore causes Flyway to silently ignore the file.

    `backend/src/main/resources/db/migration/V2__seed_admin.sql`: generate a FRESH BCrypt hash for password "Admin@123" using strength 10. Do NOT reuse any hash from documentation or examples (Pitfall 9). Write a Java snippet mentally: `new BCryptPasswordEncoder(10).encode("Admin@123")` and inline the resulting `$2a$10$...` hash. INSERT INTO users (username, email, password_hash, role) VALUES ('admin', 'admin@vdt.local', '<freshly-generated-hash>', 'ADMIN'). Document the seed password in `docs/setup.md` (created in Plan 04).

    `backend/Dockerfile`: multi-stage. Stage 1: `FROM maven:3.9-eclipse-temurin-21 AS build`, WORKDIR /app, COPY pom.xml ., `RUN mvn dependency:go-offline -B`, COPY src ./src, `RUN mvn package -DskipTests -B`. Stage 2: `FROM eclipse-temurin:21-jre-alpine`, WORKDIR /app, `COPY --from=build /app/target/*.jar app.jar`, EXPOSE 8080, `ENTRYPOINT ["java", "-jar", "app.jar"]`.

    Also create the main application class at `backend/src/main/java/com/vdt/webrtc/WebrtcApplication.java` with `@SpringBootApplication` and standard `main()`. Without this class Spring Boot cannot start. (ALREADY EXISTS — see Reconciliation note.)
  </action>
  <verify>
    <automated>cd D:/VDTWebRTC/VDT2026-WebRTC/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    `./mvnw compile` exits 0. `backend/src/main/resources/db/migration/V1__create_tables.sql` and `V2__seed_admin.sql` exist. Both migration filenames contain double underscore. `application.yaml` contains `ddl-auto: validate`. `pom.xml` contains `jjwt-api` version `0.13.0`. `pom.xml` parent version is 4.0.x (NOT 4.1.0).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend scaffold — package.json, Vite config, Tailwind CSS 4, Docker + Compose wiring</name>
  <read_first>
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-PATTERNS.md (sections: "frontend/package.json", "vite.config.ts", "frontend/Dockerfile", "frontend/nginx.conf", "docker-compose.yml")
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-UI-SPEC.md (section: Tailwind CSS 4 Configuration Notes, Design System)
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-RESEARCH.md (sections: Standard Stack frontend, Pattern 4 Vite proxy, Pattern 8 Docker Compose)
  </read_first>
  <action>
    Create the React + Vite frontend scaffold at `frontend/`.

    `frontend/package.json`: name `vdt-webrtc-frontend`, private true. Exact versions from RESEARCH.md §Standard Stack:
    - `react: 19.2.7`, `react-dom: 19.2.7`
    - `react-router: 7.17.0` (library mode, NOT react-router-dom)
    - `axios: 1.17.0`
    - `zustand: 5.0.14`
    - `@tanstack/react-query: 5.101.0`
    devDependencies:
    - `vite: 8.0.16`
    - `@vitejs/plugin-react: 6.0.2`
    - `typescript: 6.0.3`
    - `tailwindcss: 4.3.0`
    - `@tailwindcss/vite: 4.3.0`
    - `vitest: 4.1.8`
    - `@testing-library/react: 16.3.2`
    - `@testing-library/jest-dom: 6.9.1`
    - `jsdom: latest`
    scripts: `dev`, `build`, `preview`, `test` (`vitest run`), `test:ui` (`vitest --ui`).

    `frontend/tsconfig.json` and `frontend/tsconfig.app.json`: standard Vite React-TS tsconfig (target ES2022, module ESNext, moduleResolution bundler, strict true, jsx react-jsx).

    `frontend/vite.config.ts`: import `defineConfig` from `vite`, `react` from `@vitejs/plugin-react`, `tailwindcss` from `@tailwindcss/vite`. Config: `plugins: [react(), tailwindcss()]`. Server proxy: `/api` → `http://localhost:8080`, `changeOrigin: true`. This makes all API calls same-origin from the browser's perspective — eliminates CORS cookie issues in dev (RESEARCH.md Pattern 4 + Pitfall 3).

    `frontend/src/index.css`: `@import "tailwindcss";` (Tailwind 4 CSS-first import — no tailwind.config.js needed). Add CSS custom properties per UI-SPEC:
    - `--color-accent: #2563eb`
    - `--color-destructive: #dc2626`
    - `--color-surface: #ffffff`
    - `--color-canvas: #f3f4f6`
    Set base font: `font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`.

    `frontend/src/main.tsx`: standard React 19 `createRoot` mounting `<App />` into `#root`. Import `./index.css`.

    `frontend/index.html`: standard Vite HTML entry, `<div id="root">`.

    `frontend/src/App.tsx`: minimal stub — just renders `<div>VDT WebRTC — loading...</div>`. Plans 02 and 03 will replace this with the real router.

    `frontend/Dockerfile`: multi-stage. Stage 1: `FROM node:22-alpine AS build`, WORKDIR /app, COPY package*.json ., `RUN npm ci`, COPY . ., `RUN npm run build`. Stage 2: `FROM nginx:1.27-alpine`, `COPY --from=build /app/dist /usr/share/nginx/html`, `COPY nginx.conf /etc/nginx/conf.d/default.conf`, EXPOSE 80.

    `frontend/nginx.conf`: listen 80, root `/usr/share/nginx/html`, index.html. location `/api/` proxies to `http://backend:8080` with `proxy_set_header Host $host` and `proxy_set_header X-Real-IP $remote_addr`. location `/` uses `try_files $uri /index.html` (SPA fallback for React Router).

    `docker-compose.yml` (repo root) — ALREADY EXISTS with `postgres` + `backend` (quick task 260613-laz, `.env`-driven, postgres on host 5433, volume `pgdata`, wget healthcheck). DO NOT recreate or overwrite it — EXTEND it by adding ONLY the `frontend` service:
    - `frontend`: build `./frontend`, `depends_on: backend: condition: service_healthy`, ports `80:80`.
    Keep the existing postgres/backend services and `.env` wiring as-is. (Existing differences from the original plan are intentional and accepted: creds come from `.env` not inline; volume is `pgdata`; postgres published on 5433 to avoid clashing with the dev's local Postgres on 5432; healthcheck uses wget. These are fine.)

    `docker-compose.dev.yml` (repo root): per D-06, only postgres service, mapped port `5432:5432`. Used with `docker compose -f docker-compose.dev.yml up -d` for daily hot-reload dev. NOTE: the developer currently runs a LOCAL Postgres 17 on 5432 (role `root`, db `vdt_webrtc`) for daily dev, so this dev compose is an ALTERNATIVE for people without a local Postgres — do NOT run both at once (both bind 5432). This file is still a useful deliverable; create it.
  </action>
  <verify>
    <automated>cd D:/VDTWebRTC/VDT2026-WebRTC/frontend && npm install --silent 2>&1 | tail -3 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm install` exits 0. `npm run build` produces `frontend/dist/index.html`. `vite.config.ts` contains proxy `/api` pointing to `localhost:8080`. `docker-compose.yml` contains `condition: service_healthy` and `postgres_data` volume. `docker-compose.dev.yml` exists and only defines the `postgres` service.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wave 0 test stubs — backend Testcontainers tests + frontend Vitest stubs</name>
  <read_first>
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-RESEARCH.md (section: Validation Architecture — Phase Requirements → Test Map, Wave 0 Gaps)
    D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-VALIDATION.md
  </read_first>
  <action>
    Create all Wave 0 test stub files so subsequent plan tasks have a green test baseline. All tests must PASS trivially on the stub (e.g., @Disabled, empty test body, or simple assertion on a value that always holds). Plans 02–04 will fill in the real assertions.

    `backend/src/test/java/com/vdt/webrtc/auth/AuthControllerTest.java`: annotate `@SpringBootTest(webEnvironment = RANDOM_PORT)`, `@AutoConfigureMockMvc`, `@Testcontainers`. Inject a `@Container` field: `static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:17-alpine")`. Annotate with `@ServiceConnection`. Declare test method stubs (each annotated `@Test @Disabled("stub — implemented in Plan 02")`) for: `register_success_returns_201`, `register_duplicate_email_returns_409`, `register_duplicate_username_returns_409`, `register_invalid_dto_returns_400`, `login_valid_credentials_returns_access_token_and_cookie`, `login_invalid_credentials_returns_401`, `refresh_valid_cookie_rotates_token`, `refresh_revoked_token_returns_401`, `logout_clears_cookie_and_revokes_token`, `after_logout_refresh_returns_401`. No assertions yet.

    `backend/src/test/java/com/vdt/webrtc/config/SecurityConfigTest.java`: annotate `@SpringBootTest(webEnvironment = RANDOM_PORT)`, `@AutoConfigureMockMvc`, `@Testcontainers`. `@ServiceConnection` PostgreSQLContainer. Stub test methods: `admin_endpoint_with_user_role_returns_403`, `admin_endpoint_with_admin_role_returns_200`, `auth_endpoints_are_public`. All `@Disabled`.

    `backend/src/test/java/com/vdt/webrtc/FlywayMigrationTest.java`: annotate `@SpringBootTest`, `@Testcontainers`. `@ServiceConnection` PostgreSQLContainer. One ACTIVE (not disabled) test method `flyway_migrations_apply_cleanly`: inject `JdbcTemplate`, run `SELECT COUNT(*) FROM flyway_schema_history`, assert result equals 2. This test should PASS immediately after Task 1 (the migrations exist). Also add stub `seed_admin_exists_in_users` annotated `@Disabled`.

    `frontend/vitest.config.ts`: use `defineConfig` from `vitest/config`. Set `test.environment: 'jsdom'`, `test.globals: true`, `test.setupFiles: ['./src/test/setup.ts']`. Also create `frontend/src/test/setup.ts`: `import '@testing-library/jest-dom'`.

    `frontend/src/api/axiosInstance.test.ts`: import `describe`, `it`, `expect` from `vitest`. Add `describe('axiosInstance 401 interceptor', () => { it.todo('queues concurrent requests and only calls refresh once') })`. No real assertions yet.

    `frontend/src/auth/AuthContext.test.tsx`: `describe('AuthContext', () => { it.todo('restores session on mount by calling /auth/refresh') })`.

    `frontend/src/auth/ProtectedRoute.test.tsx`: `describe('ProtectedRoute', () => { it.todo('redirects unauthenticated user to /login') it.todo('redirects USER role to / when requiredRole=ADMIN') })`.
  </action>
  <verify>
    <automated>cd D:/VDTWebRTC/VDT2026-WebRTC && docker compose -f docker-compose.dev.yml up -d postgres --wait 2>&1 | tail -3 && ./backend/mvnw test -pl backend -Dtest=FlywayMigrationTest -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    `FlywayMigrationTest#flyway_migrations_apply_cleanly` PASSES (count of flyway_schema_history rows equals 2). `./backend/mvnw test -pl backend` runs with only the non-disabled tests passing. `cd frontend && npx vitest run` exits 0 (todos and no-assertion tests pass trivially). All 7 stub files exist in the correct package paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Docker Compose | JWT_SECRET env var must not be committed to source; `.env` file in .gitignore |
| Migration files → PostgreSQL | SQL in V1/V2 is trusted (server-side); still must avoid reusing known example BCrypt hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | V2__seed_admin.sql (admin password) | mitigate | Generate FRESH BCrypt hash in Task 1 (Pitfall 9). Document in docs/setup.md. Never commit plaintext seed password. |
| T-01-02 | Information Disclosure | JWT_SECRET in docker-compose.yml | mitigate | Default placeholder `change-this-in-production-min-32-chars-base64` makes the risk visible. Production deployments MUST override via `.env` file (add `.env` to `.gitignore`). |
| T-01-03 | Tampering | Flyway migration files | accept | Migrations are version-controlled SQL; no user input reaches them at scaffold time. Risk: low. |
| T-01-SC | Tampering | npm/Maven package installs | mitigate | All npm packages verified via `npm view` per Package Legitimacy Audit in RESEARCH.md (all Approved). Maven packages from Spring BOM (trusted source). slopcheck ran but used wrong ecosystem; direct verification performed. |
</threat_model>

<verification>
1. `cd backend && ./mvnw compile` exits 0
2. `cd frontend && npm run build` exits 0
3. `FlywayMigrationTest#flyway_migrations_apply_cleanly` PASSES when run against dockerized postgres
4. `docker compose config` exits 0 (valid Compose file)
5. `cat backend/src/main/resources/db/migration/V1__create_tables.sql | grep -c "CREATE TABLE"` returns 2 (users + refresh_tokens)
6. `grep -v '^--' backend/src/main/resources/db/migration/V1__create_tables.sql | grep -c "VARCHAR(100)"` returns 1 (password_hash column — Pitfall 5 prevention)
7. `grep "ddl-auto" backend/src/main/resources/application.yaml` returns `ddl-auto: validate`
8. `grep "4\.0\." backend/pom.xml` returns match on spring-boot-starter-parent version (confirming NOT 4.1.x)
</verification>

<success_criteria>
- Backend compiles cleanly with Java 21 and Spring Boot 4.0.x BOM (latest 4.0 patch — NOT 4.1.0)
- Frontend builds (`npm run build` exits 0) with React 19, Vite 8, Tailwind 4
- Flyway V1 and V2 migrations both named with double underscore
- `FlywayMigrationTest#flyway_migrations_apply_cleanly` passes against a real PostgreSQL container (exactly 2 rows in flyway_schema_history)
- `docker compose up` (full stack) starts without errors; backend health check returns 200
- All Wave 0 test stub files exist; `vitest run` and `mvnw test` both exit 0
- `application.yaml` has `ddl-auto: validate` (NEVER create/update)
- V2 seed hash is freshly generated (not a copy of documentation examples)
- springdoc-openapi 3.0.3 included in pom.xml; if compile fails on it, it is commented out with a note in docs/setup.md
</success_criteria>

<output>
Create `D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/01-foundation-auth-roles-project-skeleton/01-01-SUMMARY.md` when done
</output>
