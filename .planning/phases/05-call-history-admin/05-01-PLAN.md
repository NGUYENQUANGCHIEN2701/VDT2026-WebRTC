---
phase: 05-call-history-admin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java
  - backend/src/test/java/com/vdt/webrtc/history/CallHistoryConsumerTest.java
  - backend/src/test/java/com/vdt/webrtc/history/CallHistoryPublisherTest.java
  - backend/src/test/java/com/vdt/webrtc/history/CallHistoryApiTest.java
  - backend/src/test/java/com/vdt/webrtc/admin/AdminLockTest.java
  - backend/src/test/java/com/vdt/webrtc/admin/AdminLockWsTest.java
  - backend/src/test/java/com/vdt/webrtc/admin/AdminServiceTest.java
  - backend/src/test/java/com/vdt/webrtc/admin/AdminHistoryApiTest.java
  - backend/src/test/java/com/vdt/webrtc/admin/AdminDashboardApiTest.java
  - backend/src/test/java/com/vdt/webrtc/call/CallServicePublishTest.java
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - ADMN-01
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "All Phase 5 test files compile but fail RED before any implementation is written"
    - "RabbitMQContainer wired via @ServiceConnection in TestcontainersConfiguration — integration tests can start a real broker"
    - "testcontainers:rabbitmq module is in pom.xml test scope so RabbitMQContainer class resolves"
    - "Each test file covers exactly the requirement rows listed in 05-VALIDATION.md Wave 0 gaps"
  artifacts:
    - path: "backend/pom.xml"
      provides: "org.testcontainers:rabbitmq test-scope dependency"
      contains: "testcontainers.*rabbitmq"
    - path: "backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java"
      provides: "RabbitMQContainer @ServiceConnection bean"
      contains: "RabbitMQContainer"
    - path: "backend/src/test/java/com/vdt/webrtc/history/CallHistoryConsumerTest.java"
      provides: "RED tests for HIST-01 async pipeline and HIST-03 idempotency + DLQ"
    - path: "backend/src/test/java/com/vdt/webrtc/history/CallHistoryPublisherTest.java"
      provides: "RED tests for HIST-01 fire-and-forget (publish always forwards to AmqpTemplate regardless of endReason)"
    - path: "backend/src/test/java/com/vdt/webrtc/call/CallServicePublishTest.java"
      provides: "RED unit test asserting busy branch in CallService triggers zero publisher.publish() calls (D-05/HIST-01)"
    - path: "backend/src/test/java/com/vdt/webrtc/history/CallHistoryApiTest.java"
      provides: "RED tests for HIST-02 user history endpoint + cursor pagination"
    - path: "backend/src/test/java/com/vdt/webrtc/admin/AdminLockTest.java"
      provides: "RED tests for ADMN-01 lock REST + future login block"
    - path: "backend/src/test/java/com/vdt/webrtc/admin/AdminLockWsTest.java"
      provides: "RED tests for ADMN-01 WS force-close on lock"
    - path: "backend/src/test/java/com/vdt/webrtc/admin/AdminServiceTest.java"
      provides: "RED unit test for ADMN-01 self-protection (cannot lock self)"
    - path: "backend/src/test/java/com/vdt/webrtc/admin/AdminHistoryApiTest.java"
      provides: "RED tests for ADMN-02 system-wide history endpoint"
    - path: "backend/src/test/java/com/vdt/webrtc/admin/AdminDashboardApiTest.java"
      provides: "RED tests for ADMN-03 dashboard counts endpoint"
  key_links:
    - from: "TestcontainersConfiguration.java"
      to: "RabbitMQContainer"
      via: "@ServiceConnection"
      pattern: "@ServiceConnection.*RabbitMQContainer"
    - from: "CallHistoryConsumerTest.java"
      to: "TestcontainersConfiguration"
      via: "@Import"
      pattern: "@Import\\(TestcontainersConfiguration"
---

<objective>
Wave 0 — Red test scaffolding and infra prerequisites.

This plan adds the testcontainers:rabbitmq dependency to pom.xml, extends
TestcontainersConfiguration with a RabbitMQContainer bean, and creates all eight
failing test classes specified in 05-VALIDATION.md Wave 0. The tests must compile
but fail RED because the production code does not exist yet (HIST-01/02/03,
ADMN-01/02/03 classes are absent).

Purpose: Establish the Nyquist sampling harness before writing a single line of
production code. Every subsequent plan wave can run its targeted tests immediately
after implementation. Keeps the "sampling rate" contract from 05-VALIDATION.md.

Output: pom.xml with rabbitmq TC module, updated TestcontainersConfiguration, 8
RED test files, all compilable via `./mvnw test-compile -pl backend`.
</objective>

<execution_context>
@D:\VDTWebRTC\VDT2026-WebRTC\.planning\phases\05-call-history-admin\05-VALIDATION.md
@D:\VDTWebRTC\VDT2026-WebRTC\.planning\phases\05-call-history-admin\05-RESEARCH.md
@D:\VDTWebRTC\VDT2026-WebRTC\.planning\phases\05-call-history-admin\05-PATTERNS.md
</execution_context>

<context>
@D:\VDTWebRTC\VDT2026-WebRTC\.planning\ROADMAP.md
@D:\VDTWebRTC\VDT2026-WebRTC\.planning\phases\05-call-history-admin\05-CONTEXT.md

<!-- Test infra analogs -->
@D:\VDTWebRTC\VDT2026-WebRTC\backend\src\test\java\com\vdt\webrtc\TestcontainersConfiguration.java
@D:\VDTWebRTC\VDT2026-WebRTC\backend\pom.xml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RabbitMQ Testcontainers dependency and extend TestcontainersConfiguration</name>

  <read_first>
    Read backend/pom.xml to find the existing testcontainers block (the PostgreSQL + Redis
    dependencies, near the bottom of the file) so you add in the same style.

    Read backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java to see the
    existing @Bean pattern for PostgreSQLContainer and GenericContainer (Redis) so the
    RabbitMQContainer bean follows the same structure exactly.

    From 05-PATTERNS.md §TestcontainersConfiguration: the RabbitMQContainer bean uses
    @ServiceConnection directly (no `name` attribute — unlike the GenericContainer for Redis
    which uses @ServiceConnection(name = "redis")).
  </read_first>

  <files>
    backend/pom.xml,
    backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java
  </files>

  <behavior>
    - pom.xml gets a new dependency: groupId org.testcontainers, artifactId rabbitmq, scope test, no version element (managed by spring-boot-testcontainers BOM)
    - TestcontainersConfiguration gets a new @Bean method rabbitMqContainer() returning RabbitMQContainer("rabbitmq:4.1-management") annotated with @Bean and @ServiceConnection
    - Import: org.testcontainers.containers.rabbitmq.RabbitMQContainer
  </behavior>

  <action>
    In backend/pom.xml, locate the Testcontainers test-scope dependencies block (near the
    existing postgresql/rabbitmq/generic containers). Add a new dependency entry:
    groupId=org.testcontainers, artifactId=rabbitmq, scope=test. No version element — the
    spring-boot-testcontainers BOM manages it.

    In TestcontainersConfiguration.java, add the import for
    org.testcontainers.containers.rabbitmq.RabbitMQContainer. Add a new @Bean method:

      @Bean
      @ServiceConnection
      RabbitMQContainer rabbitMqContainer() {
          return new RabbitMQContainer("rabbitmq:4.1-management");
      }

    Do NOT add a name attribute to @ServiceConnection — RabbitMQContainer is detected by
    type, unlike the Redis GenericContainer which needs name="redis".

    Do NOT change the existing PostgreSQLContainer or GenericContainer (Redis) beans.
  </action>

  <verify>
    <automated>cd /d/VDTWebRTC/VDT2026-WebRTC && ./mvnw test-compile -pl backend -q 2>&1 | tail -5</automated>
  </verify>

  <done>
    ./mvnw test-compile -pl backend succeeds (exit 0). The RabbitMQContainer import resolves.
    pom.xml contains org.testcontainers:rabbitmq in test scope. TestcontainersConfiguration
    has the rabbitMqContainer() @Bean with @ServiceConnection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all 8 RED test stubs (Wave 0 gap files from 05-VALIDATION.md)</name>

  <read_first>
    Read 05-VALIDATION.md §Wave 0 Requirements for the complete list of test files and which
    requirement each covers.

    Read 05-RESEARCH.md §Validation Architecture §Key Integration Test Patterns for the
    Awaitility-based async assertion pattern and the idempotency test structure.

    Read backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java (just updated
    in Task 1) to confirm the exact @Import class name.

    Read backend/src/test/java/com/vdt/webrtc/ directory structure to understand the package
    layout pattern used by existing test classes.

    Read 05-PATTERNS.md §TestcontainersConfiguration for the @Import pattern used in
    integration tests.
  </read_first>

  <files>
    backend/src/test/java/com/vdt/webrtc/history/CallHistoryConsumerTest.java,
    backend/src/test/java/com/vdt/webrtc/history/CallHistoryPublisherTest.java,
    backend/src/test/java/com/vdt/webrtc/history/CallHistoryApiTest.java,
    backend/src/test/java/com/vdt/webrtc/admin/AdminLockTest.java,
    backend/src/test/java/com/vdt/webrtc/admin/AdminLockWsTest.java,
    backend/src/test/java/com/vdt/webrtc/admin/AdminServiceTest.java,
    backend/src/test/java/com/vdt/webrtc/admin/AdminHistoryApiTest.java,
    backend/src/test/java/com/vdt/webrtc/admin/AdminDashboardApiTest.java,
    backend/src/test/java/com/vdt/webrtc/call/CallServicePublishTest.java
  </files>

  <behavior>
    Each file must: (a) compile successfully, (b) fail RED when run because the production
    classes they reference do not exist yet (the imports will fail to resolve — use
    @Disabled("Wave 0 stub — implementation pending") on each @Test method so the class
    compiles even though production classes are absent, OR stub the missing types as empty
    placeholder annotations in comments and rely on compilation failing with a clear
    "class not found" error that is meaningful not accidental).

    PREFERRED approach: Write each test class as a @SpringBootTest shell with real @Test
    method signatures referencing the target production class names. Since the production
    classes do not exist, the tests will FAIL TO COMPILE. That is the correct RED state for
    Wave 0 — the compile failure IS the red test. The executor can note
    "BUILD FAILURE — cannot find symbol: CallHistoryPublisher" etc. as proof of RED.

    Actually the BETTER approach per project conventions (Phase 4 pattern): create each test
    class with the correct package, @SpringBootTest (or plain JUnit @ExtendWith for unit
    tests), and @Test methods whose bodies throw new UnsupportedOperationException("not
    yet implemented"). Import the target production class. The test will either fail to
    compile (production class absent) or fail at runtime with UnsupportedOperationException.
    Either way the test is RED.

    File-by-file specifications:

    CallHistoryConsumerTest (package com.vdt.webrtc.history):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class)
    - @Autowired CallHistoryRepository repo, @Autowired CallHistoryPublisher publisher
    - @Test publishedEvent_persistedWithinTimeout: publisher.publish(event); Awaitility await 5s
      untilAsserted repo.findByCallId("call-123").hasSize(2). Covers HIST-01 + HIST-03 pipeline.
    - @Test duplicateDelivery_producesOnlyOneRow: publish twice, await repo.findByCallId hasSize 2. Covers HIST-03 idempotency.
    - @Test consumerFailure_routesToDlq: simulate failure; assert DLQ has message within 10s. Covers HIST-03 DLQ.

    CallHistoryPublisherTest (package com.vdt.webrtc.history):
    - Plain JUnit 5 + Mockito (@ExtendWith(MockitoExtension.class))
    - @Mock AmqpTemplate amqpTemplate, @InjectMocks CallHistoryPublisher publisher
    - @Test publish_callsAmqpTemplate: verify amqpTemplate.convertAndSend called once. Covers HIST-01 fire-and-forget.
    - @Test publish_swallowsAmqpException: doThrow AmqpException; publisher.publish(event); no exception propagated. Covers HIST-01 non-blocking.
    - @Test publish_forwardsRegardlessOfEndReason: call publisher.publish() with each of the 5 valid endReasons (completed, missed, rejected, cancelled, dropped); each invokes amqpTemplate.convertAndSend exactly once. CallHistoryPublisher is a generic forwarder and MUST NOT filter by endReason. Covers HIST-01.
    NOTE (B4 fix): busy exclusion belongs in CallService (which never calls publisher.publish() in the busy branch), not in CallHistoryPublisherTest.

    CallServicePublishTest (package com.vdt.webrtc.call):
    - Plain JUnit 5 + Mockito (@ExtendWith(MockitoExtension.class))
    - @Mock CallHistoryPublisher callHistoryPublisher, plus all other CallService dependencies (CallStateRepository, broadcaster, etc.) as mocks
    - @Test busyBranch_neverPublishesHistory: invoke the BUSY branch of CallService.handleInvite() (mock the target user as busy); assert Mockito.verify(callHistoryPublisher, Mockito.never()).publish(any()). Covers D-05/HIST-01 (busy exclusion is in CallService, not in CallHistoryPublisher).

    CallHistoryApiTest (package com.vdt.webrtc.history):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class)
    - @Test getHistory_returnsOwnHistoryNewestFirst: insert 2 rows for viewer, GET /api/history, assert order by endedAt desc + correct direction. Covers HIST-02.
    - @Test getHistory_cursorPagination: insert 25 rows, fetch page 1 (size=20), assert nextCursor present; fetch page 2, assert 5 rows + nextCursor null. Covers HIST-02.
    - @Test getHistory_rejectsUnauthenticated: GET /api/history without token, assert 401/403. Security check.

    AdminLockTest (package com.vdt.webrtc.admin):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class)
    - @Test lockUser_setsLockedTrue: PATCH /api/admin/users/{id}/lock, assert User.isLocked() == true in DB. Covers ADMN-01.
    - @Test lockedUser_cannotLogin: lock user, POST /api/auth/login, assert 403 (accountLocked). Covers ADMN-01.
    - @Test unlockUser_allowsLogin: unlock user, POST /api/auth/login, assert 200. Covers ADMN-01.
    - @Test nonAdmin_lockEndpoint_returns403: regular user PATCH /api/admin/users/{id}/lock, assert 403. Security.

    AdminLockWsTest (package com.vdt.webrtc.admin):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class) + StandardWebSocketClient
    - @Test lockUser_closesWsSession: connect target user WS, PATCH lock, assert session closed within 3s (Awaitility). Covers ADMN-01 force-disconnect (D-11).

    AdminServiceTest (package com.vdt.webrtc.admin):
    - Plain JUnit 5 + Mockito (@ExtendWith(MockitoExtension.class))
    - @Mock UserRepository, @Mock SessionRegistry, @InjectMocks AdminService
    - @Test lockSelf_throwsIllegalArgument: adminService.lockUser("admin", "admin") throws IllegalArgumentException. Covers D-10 / ADMN-01.
    - @Test changeRoleSelf_throwsIllegalArgument: adminService.changeRole("admin", adminId, "USER") throws IllegalArgumentException. Covers D-10.

    AdminHistoryApiTest (package com.vdt.webrtc.admin):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class)
    - @Test adminHistory_returnsAllCalls: insert rows for multiple viewers, GET /api/admin/history, assert all rows returned. Covers ADMN-02.
    - @Test adminHistory_filterByUsername: GET /api/admin/history?username=alice, assert only alice's calls. Covers ADMN-02 + D-12.
    - @Test adminHistory_regularUser_returns403. Security.

    AdminDashboardApiTest (package com.vdt.webrtc.admin):
    - @SpringBootTest(webEnvironment = RANDOM_PORT) + @Import(TestcontainersConfiguration.class)
    - @Test dashboard_returnsExpectedFields: GET /api/admin/dashboard, assert JSON has keys: onlineUsers, activeCalls, todayStarted, todayCompleted, todayMissed. Covers ADMN-03.
    - @Test dashboard_regularUser_returns403. Security.
  </action>

  <verify>
    <automated>cd /d/VDTWebRTC/VDT2026-WebRTC && ./mvnw test -pl backend -Dtest="CallHistoryConsumerTest,CallHistoryPublisherTest,AdminServiceTest" -DfailIfNoTests=false 2>&1 | grep -E "(BUILD|ERROR|FAILURE|Tests run)" | head -20</automated>
  </verify>

  <done>
    All 8 test files exist under backend/src/test/java/com/vdt/webrtc/history/ and
    backend/src/test/java/com/vdt/webrtc/admin/. Running any of them fails with a clear
    error (compilation failure "cannot find symbol" or runtime UnsupportedOperationException).
    The test framework sees them as test classes. No test is accidentally green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Application | Integration tests interact with the running Spring context via real HTTP and WebSocket; test-scope beans (RabbitMQContainer) must not leak into production scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | TestcontainersConfiguration — RabbitMQContainer image | mitigate | Pin image to rabbitmq:4.1-management (same as docker-compose) — do not use :latest |
| T-05-SC | Tampering | org.testcontainers:rabbitmq Maven dependency | mitigate | Package legitimacy audit in RESEARCH.md §Package Legitimacy Audit confirmed Approved via java.testcontainers.org; no slopcheck gate triggered (Maven artifact) |
</threat_model>

<verification>
1. `./mvnw test-compile -pl backend` exits 0 — all 8 test files compile (production classes absent → imports fail to resolve → this is expected; annotate each @Test with @Disabled to allow compilation)
2. Running any test file with `./mvnw test -pl backend -Dtest=AdminServiceTest` fails at setup (class not found or UnsupportedOperationException) — the RED state is confirmed
3. pom.xml contains `<artifactId>rabbitmq</artifactId>` in test scope: `grep -A2 "testcontainers" backend/pom.xml | grep -v "^--" | grep "rabbitmq"`
4. TestcontainersConfiguration.java contains RabbitMQContainer: `grep "RabbitMQContainer" backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java`
</verification>

<success_criteria>
- pom.xml: org.testcontainers:rabbitmq in test scope (no version tag — BOM managed)
- TestcontainersConfiguration.java: rabbitMqContainer() @Bean with @ServiceConnection returning RabbitMQContainer("rabbitmq:4.1-management")
- 9 test files created: 8 covering 05-VALIDATION.md Wave 0 Requirements + CallServicePublishTest covering D-05/HIST-01 busy exclusion at CallService level
- All test files are in the correct package: com.vdt.webrtc.history or com.vdt.webrtc.admin
- No test is accidentally passing green (all RED or compile-error)
- `./mvnw test-compile -pl backend` exits 0 after adding @Disabled to test methods whose imports are unresolvable
</success_criteria>

<output>
Create `.planning/phases/05-call-history-admin/05-01-SUMMARY.md` when done.
</output>
