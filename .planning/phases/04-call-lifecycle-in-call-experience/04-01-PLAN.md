---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/vdt/webrtc/ws/CallLifecycleTest.java
  - backend/src/test/java/com/vdt/webrtc/call/CallStateMachineTest.java
  - backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java
  - frontend/src/store/callStore.test.ts
  - frontend/src/realtime/mediaControls.test.ts
autonomous: true
requirements:
  - CALL-02
  - CALL-03
  - CALL-04
  - CALL-05
  - CALL-06
  - CALL-07
  - CALL-08
  - MEDIA-01
  - STAB-01
  - STAB-02

must_haves:
  truths:
    - "Wave 0 test files exist with RED (failing) tests that document every concurrency behavior: glare, busy, missed-timeout, grace-drop, and concurrent CAS"
    - "TestcontainersConfiguration extends to include a Redis container wired via @ServiceConnection(name='redis')"
    - "All test classes compile and fail predictably — no missing-class compilation errors"
  artifacts:
    - path: "backend/src/test/java/com/vdt/webrtc/ws/CallLifecycleTest.java"
      provides: "2-client WS integration harness covering CALL-02..08, STAB-01, STAB-02, MEDIA-01"
      min_lines: 100
    - path: "backend/src/test/java/com/vdt/webrtc/call/CallStateMachineTest.java"
      provides: "Unit harness for CAS atomicity (CALL-08)"
      min_lines: 40
    - path: "backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java"
      provides: "Redis + Postgres Testcontainers config"
      contains: "@ServiceConnection(name = \"redis\")"
    - path: "frontend/src/store/callStore.test.ts"
      provides: "Vitest RED tests for new callStore state fields + endCall action"
      min_lines: 30
    - path: "frontend/src/realtime/mediaControls.test.ts"
      provides: "Vitest RED tests for track.enabled toggle and callStore updates"
      min_lines: 20
  key_links:
    - from: "backend/src/test/java/com/vdt/webrtc/ws/CallLifecycleTest.java"
      to: "WsTestSupport.awaitMatching"
      via: "extends WsTestSupport"
      pattern: "extends WsTestSupport"
    - from: "backend/src/test/java/com/vdt/webrtc/call/CallStateMachineTest.java"
      to: "TestcontainersConfiguration"
      via: "@Import(TestcontainersConfiguration.class)"
      pattern: "@Import\\(TestcontainersConfiguration\\.class\\)"
---

## Phase Goal

**As a** developer, **I want to** have failing tests that specify every call lifecycle behavior, **so that** subsequent implementation waves have a safety net and the passing criteria are unambiguous before any production code is written.

<objective>
Create the Wave 0 test scaffold for Phase 4. This is the RED phase: tests must exist, compile, and fail with a clear "class not found" or "method not found" error — not with NPEs or runtime confusion. The tests define the contract that implementation plans (Plans 02-07) must satisfy.

Purpose: Establish Nyquist-compliant coverage gate. Without these tests the concurrent behaviors (glare, busy, grace expiry) cannot be verified automatically. Tests prevent regression as each wave lands.

Output: 5 test files covering all 11 requirements. Redis Testcontainers wiring for CallStateMachineTest and CallLifecycleTest.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-call-lifecycle-in-call-experience/04-CONTEXT.md
@.planning/phases/04-call-lifecycle-in-call-experience/04-RESEARCH.md
@.planning/phases/04-call-lifecycle-in-call-experience/04-PATTERNS.md
@.planning/phases/04-call-lifecycle-in-call-experience/04-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Wave 0 test scaffold — CallLifecycleTest + CallStateMachineTest + Testcontainers Redis</name>
  <files>
    backend/src/test/java/com/vdt/webrtc/ws/CallLifecycleTest.java,
    backend/src/test/java/com/vdt/webrtc/call/CallStateMachineTest.java,
    backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java
  </files>
  <read_first>
    - backend/src/test/java/com/vdt/webrtc/ws/CallSignalingTest.java (exact analog for CallLifecycleTest structure — extends WsTestSupport, mintToken, connect, sendMessage, awaitMatching pattern)
    - backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java (awaitMatching(Predicate, long) signature, CollectingHandler, connect, mintToken)
    - backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java (current contents — add Redis container without removing Postgres)
    - 04-RESEARCH.md lines 737-751 (Testcontainers Redis @ServiceConnection(name="redis") + GenericContainer pattern — Pitfall 6)
    - 04-RESEARCH.md lines 860-894 (Phase Requirements → Test Map — exact test method names and assertions)
    - 04-PATTERNS.md lines 1208-1294 (CallLifecycleTest and CallStateMachineTest exact structure)
  </read_first>
  <behavior>
    - CallLifecycleTest.invite_triggers_ringing: connect bob+alice, alice sends call-invite, bob receives call-state-changed with state=ringing and callerUserId=alice — RED: CallService not yet wired
    - CallLifecycleTest.caller_cancel_ends_call: alice invites bob (gets ringing), alice sends call-cancel, both receive ended{cancelled} — RED
    - CallLifecycleTest.ring_timeout_missed (use @TestPropertySource("call.ring-timeout-seconds=2")): invite, wait 3s, both get ended{missed} — RED
    - CallLifecycleTest.busy_rejection: put bob in active call first, alice invites bob, alice gets ended{busy}, bob never notified — RED
    - CallLifecycleTest.glare_resolution: alice+bob send simultaneous call-invites, exactly one call established (lower userId wins), both get ringing for that call — RED
    - CallLifecycleTest.hangup_both_notified: full invite→accept→hangup flow, both get ended{completed} — RED
    - CallLifecycleTest.media_state_relay: in active call, alice sends media-state, bob gets media-state-relay — RED
    - CallLifecycleTest.ws_reconnect_resync: establish call, bob disconnects, bob reconnects and sends state-resync, gets state-resync-ack with currentCall.state=active — RED
    - CallLifecycleTest.grace_period_rejoin: bob disconnects, reconnects in <2s (test grace), call NOT ended — RED
    - CallLifecycleTest.grace_period_drop: bob disconnects, no reconnect, after test grace expiry both get ended{dropped} — RED
    - CallStateMachineTest.concurrent_transition_only_one_winner: create ringing call in Redis (use StringRedisTemplate directly), two threads simultaneously call transition("ringing","active"), assert exactly one returns true — RED: CallStateMachine not yet wired
    - TestcontainersConfiguration.java: add GenericContainer("redis:7-alpine").withExposedPorts(6379) with @Bean @ServiceConnection(name="redis") — must compile immediately (GenericContainer in testcontainers:testcontainers is already on classpath)
  </behavior>
  <action>
    Extend TestcontainersConfiguration.java: import org.testcontainers.containers.GenericContainer and org.springframework.boot.testcontainers.service.connection.ServiceConnection. Add a @Bean @ServiceConnection(name = "redis") GenericContainer method returning new GenericContainer("redis:7-alpine").withExposedPorts(6379). Do NOT remove the existing PostgreSQLContainer bean.

    Create CallLifecycleTest.java in package com.vdt.webrtc.ws extending WsTestSupport. Annotate with @TestPropertySource(properties = {"call.ring-timeout-seconds=2", "call.grace-period-seconds=2"}) to keep timer tests fast. Add 10 test methods per the behavior list above. Each test connects clients via mintToken + connect(token, handler). Assert expected call-state-changed JSON frames using awaitMatching(frame -> frame.contains("call-state-changed") && frame.contains(expectedState), timeoutMs). For timeout-based tests, awaitMatching with 5000ms. Tests must fail because CallService/CallStateMachine classes do not yet exist — the compiler error from class-not-found will be resolved in Plan 02.

    Create CallStateMachineTest.java in package com.vdt.webrtc.call. Annotate @SpringBootTest @Import(TestcontainersConfiguration.class). Autowire CallStateMachine (will be missing — test will fail at autowiring, which is acceptable RED state). Write concurrent_transition_only_one_winner: create 2 threads, both call stateMachine.transition(callId, "ringing", "active", null, "alice", "bob"), use CountDownLatch to start simultaneously, assert only one returns true via AtomicInteger counter.

    Use tools.jackson.* imports (NOT com.fasterxml.*) if any JSON needed in tests — per 04-PATTERNS.md shared patterns ObjectMapper section.
  </action>
  <verify>
    <automated>cd backend && ./mvnw -q test -Dtest="CallLifecycleTest,CallStateMachineTest" 2>&1 | tail -20</automated>
  </verify>
  <done>TestcontainersConfiguration compiles without error. CallLifecycleTest and CallStateMachineTest exist and produce compilation failures or autowiring failures (not runtime NPEs from wrong logic). Running the test command shows "BUILD FAILURE" with class-not-found or bean-not-found — not "BUILD SUCCESS".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend Wave 0 test scaffold — callStore new fields + mediaControls track.enabled stubs</name>
  <files>
    frontend/src/store/callStore.test.ts,
    frontend/src/realtime/mediaControls.test.ts
  </files>
  <read_first>
    - frontend/src/store/callStore.ts (current file — lines 1-43, existing CallState type, existing fields and actions to understand what's already there)
    - frontend/src/store/presenceStore.ts (Zustand selector + test pattern analog)
    - 04-PATTERNS.md lines 616-684 (callStore.ts extended type additions — EndReason, extended CallStoreState, endCall action shape)
    - 04-RESEARCH.md lines 757-779 (callStore.ts additions — exact type signatures for EndReason, new fields)
    - 04-PATTERNS.md lines 762-799 (mediaControls.ts — toggleMic/toggleCam shape)
    - frontend/vitest.config.ts (existing config — import environment settings)
  </read_first>
  <behavior>
    - callStore.test.ts: endCall('completed', 120) sets callState='ended', endReason='completed', callDurationSec=120 — RED: endCall action not yet in store
    - callStore.test.ts: endCall('dropped') sets callState='ended', endReason='dropped', callDurationSec=null — RED
    - callStore.test.ts: setMicMuted(true) sets micMuted=true — RED: setMicMuted not yet in store
    - callStore.test.ts: reset() clears all new fields to default (endReason=null, micMuted=false, camOff=false, etc.) — RED
    - callStore.test.ts: initial state has endReason=null, micMuted=false, camOff=false, remoteMicMuted=false, remoteCamOff=false — RED
    - mediaControls.test.ts: toggleMic() when audioTrack exists: calls track.enabled toggle + calls setMicMuted — RED: mediaControls module not yet created
    - mediaControls.test.ts: toggleCam() when videoTrack exists: calls track.enabled toggle + calls setCamOff — RED
  </behavior>
  <action>
    Create frontend/src/store/callStore.test.ts importing useCallStore from './callStore'. Use Vitest (import { describe, it, expect, beforeEach } from 'vitest'). In beforeEach, call useCallStore.getState().reset(). Write 5 tests:
    1. "initial state has new fields with defaults" — getState() and assert endReason===null, micMuted===false, camOff===false, remoteMicMuted===false, remoteCamOff===false, callDurationSec===null.
    2. "endCall sets ended state with reason and duration" — call endCall('completed', 120), assert callState==='ended', endReason==='completed', callDurationSec===120.
    3. "endCall dropped has no duration" — call endCall('dropped'), assert endReason==='dropped', callDurationSec===null.
    4. "setMicMuted toggles micMuted" — call setMicMuted(true), assert micMuted===true.
    5. "reset clears all new fields" — set some values, call reset(), assert all new fields back to default.

    Create frontend/src/realtime/mediaControls.test.ts. Import toggleMic, toggleCam from './mediaControls' (module does not exist yet — import will fail, creating the RED state). Mock getLocalStream from './callActions' to return a fake stream with getAudioTracks()[0] having enabled=true and getVideoTracks()[0] having enabled=true. Write 2 tests:
    1. "toggleMic sets track.enabled to false and updates store" — call toggleMic(), assert audioTrack.enabled===false and useCallStore.getState().micMuted===true.
    2. "toggleCam sets track.enabled to false and updates store" — call toggleCam(), assert videoTrack.enabled===false and useCallStore.getState().camOff===true.

    These tests will fail at import (mediaControls.ts does not exist) or at runtime (callStore.ts missing new fields) — both are valid RED states.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/store/callStore.test.ts src/realtime/mediaControls.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and produce FAIL output — either import errors (mediaControls not found) or assertion failures (fields missing). Output shows "FAIL" not "ERROR: cannot read test file". Running "npx vitest run src/store" produces ≥5 failing tests, not 0 tests found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → backend server | Integration tests send raw WS frames; must exercise intent-validation paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | CallLifecycleTest WS frames | mitigate | Use mintToken (real JWT) for all test clients — never bypass auth in tests; validates that the auth+identity chain from Phase 2/3 works end-to-end |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — Maven GenericContainer already on classpath via testcontainers:testcontainers; no slopcheck needed |
</threat_model>

<verification>
Backend Wave 0: cd backend && ./mvnw -q test -Dtest="CallLifecycleTest,CallStateMachineTest" — must show FAIL with class/bean not found, not compilation success.
Frontend Wave 0: cd frontend && npx vitest run src/store src/realtime/mediaControls.test.ts — must show FAIL not "no tests found".
TestcontainersConfiguration: grep -c "ServiceConnection" backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java returns ≥1.
</verification>

<success_criteria>
All 5 test files exist, compile (or fail at correct import/autowire stage), and document the Phase 4 behavioral contract in RED state. TestcontainersConfiguration has Redis fixture. No tests are accidentally GREEN before implementation.
</success_criteria>

<output>
Create `.planning/phases/04-call-lifecycle-in-call-experience/04-01-SUMMARY.md` when done.
</output>
