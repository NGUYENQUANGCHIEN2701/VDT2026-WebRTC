---
phase: 03-1-1-p2p-call-core-nat-traversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/vdt/webrtc/ws/CallSignalingTest.java
  - backend/src/test/java/com/vdt/webrtc/call/TurnControllerTest.java
  - frontend/src/webrtc/PeerManager.test.ts
  - frontend/src/webrtc/media.test.ts
  - frontend/src/webrtc/stats.test.ts
autonomous: true
requirements: [CALL-01, MEDIA-02, MEDIA-05, STAB-03, STAB-04, INFR-01]
must_haves:
  truths:
    - "Five RED test files exist, each compiles/loads, and each fails for the right reason (missing production code, not a syntax error)"
    - "CallSignalingTest drives two WS clients and asserts a call-offer from alice arrives at bob with server-stamped from"
    - "TurnControllerTest independently recomputes HMAC-SHA1 and asserts the controller matches the coturn formula"
    - "Frontend tests mock RTCPeerConnection / getUserMedia / getStats and assert perfect-negotiation, error-taxonomy, and stats-parsing behavior"
  artifacts:
    - path: "backend/src/test/java/com/vdt/webrtc/ws/CallSignalingTest.java"
      provides: "Integration test for opaque signaling relay (sendToUser routes; from is server-stamped)"
      contains: "extends WsTestSupport"
    - path: "backend/src/test/java/com/vdt/webrtc/call/TurnControllerTest.java"
      provides: "Unit test for TURN ephemeral HMAC-SHA1 credential formula"
      contains: "HmacSHA1"
    - path: "frontend/src/webrtc/PeerManager.test.ts"
      provides: "Vitest unit test for perfect negotiation + candidate buffering"
    - path: "frontend/src/webrtc/media.test.ts"
      provides: "Vitest unit test for getUserMedia error taxonomy + audio-only fallback"
    - path: "frontend/src/webrtc/stats.test.ts"
      provides: "Vitest unit test for getStats RTT/bitrate/codec/ICE-type parsing"
  key_links:
    - from: "CallSignalingTest.java"
      to: "WsTestSupport.java"
      via: "extends + mintToken/connect/awaitMatching"
      pattern: "extends WsTestSupport"
    - from: "PeerManager.test.ts"
      to: "global RTCPeerConnection"
      via: "vi.stubGlobal mock"
      pattern: "stubGlobal.*RTCPeerConnection"
---

<objective>
Wave 0 test scaffolding for Phase 3. Create the five RED test files that every subsequent
implementation task verifies against. Each test must compile/load and fail for the RIGHT
reason (production code does not exist yet), not because of a syntax error.

This is the Nyquist gate: every code-producing task in plans 02-05 has an `<automated>`
target that already exists here. WebRTC media-handshake itself is manual-only (real browser,
Phase 9 adds Playwright fake-media), so these tests cover the deterministic seams: signaling
relay, TURN HMAC, perfect-negotiation logic, media error taxonomy, and getStats parsing.

Purpose: Establish the feedback loop before implementation; lock the contracts (message
shapes, HMAC formula, error-taxonomy enum) the implementation must satisfy.
Output: Five failing test files committed (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-RESEARCH.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-PATTERNS.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend RED tests — CallSignalingTest + TurnControllerTest</name>
  <files>backend/src/test/java/com/vdt/webrtc/ws/CallSignalingTest.java, backend/src/test/java/com/vdt/webrtc/call/TurnControllerTest.java</files>
  <read_first>
    - backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java (base harness: mintToken, connect, CollectingHandler, awaitMatching)
    - backend/src/test/java/com/vdt/webrtc/ws/WsIdentityTest.java (two-client + server-stamped-from assertion; lines 12-23 pattern)
    - backend/src/test/java/com/vdt/webrtc/auth/AuthControllerTest.java (controller-test conventions for TurnControllerTest)
    - backend/src/main/java/com/vdt/webrtc/ws/message/ClientMessage.java (current sealed union — the message shape the test sends)
    - backend/src/main/java/com/vdt/webrtc/config/JwtService.java (javax.crypto.Mac / SecretKeySpec / StandardCharsets style to mirror in HMAC recompute)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-RESEARCH.md (Pattern 5 TURN HMAC formula; Pattern 4 message shapes)
  </read_first>
  <action>
    Create CallSignalingTest extending WsTestSupport. Drive two clients: alice (caller) and bob (callee).
    Test testSdpRelay: alice sends a client message type "sdp" with to="bob", callId, and an opaque sdp object;
    assert bob's CollectingHandler receives a server message type "sdp" carrying from="alice" (server-stamped, NOT
    client-supplied) and the same callId + opaque sdp body unchanged. Test testCallOffer: alice sends type "call-offer"
    to="bob"; assert bob receives type "call-offer-received" with from="alice". Add a spoofing assertion: alice sends
    a body containing from="charlie"; assert the relayed from is still "alice" (mirror WsIdentityTest). Use awaitMatching
    to tolerate presence-snapshot noise.
    Create TurnControllerTest as a controller/unit test. Inject a fixed turn.secret. Call GET /api/turn-credentials as
    an authenticated principal "alice". Independently recompute base64(HMAC-SHA1(secret, username)) where username is
    "<expiry>:alice", and assert the response.credential equals it and response.urls contains a stun: and turn: entry.
    Mirror JwtService crypto style (HmacSHA1, SecretKeySpec, StandardCharsets.UTF_8).
    Both tests MUST fail now because the production code (call message records, sendToUser impl, TurnController) does
    not exist yet — failure is the success condition for this task.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./mvnw test -Dtest="CallSignalingTest,TurnControllerTest" -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - CallSignalingTest.java exists and contains "extends WsTestSupport"
    - CallSignalingTest contains testSdpRelay and testCallOffer methods and an assertion that a spoofed from is overridden to the authenticated principal
    - TurnControllerTest.java exists and recomputes HMAC-SHA1 independently (contains "HmacSHA1") rather than trusting the controller output blindly
    - Running both tests fails (compile may pass; assertions/missing-bean fail) — exit code non-zero, proving RED
    - No test references com.fasterxml ObjectMapper for serialization (annotations-only exception allowed)
  </acceptance_criteria>
  <done>Both backend test files exist, compile against existing harness, and fail because production code is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend RED tests — PeerManager, media, stats</name>
  <files>frontend/src/webrtc/PeerManager.test.ts, frontend/src/webrtc/media.test.ts, frontend/src/webrtc/stats.test.ts</files>
  <read_first>
    - frontend/src/realtime/wsClient.test.ts (mock-global + vi.hoisted store-mock + fake-timers template — copy verbatim shape)
    - frontend/src/store/presenceStore.ts (Zustand store shape that callStore will mirror; mock target pattern)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-RESEARCH.md (Pattern 1 perfect negotiation, Pattern 2 buffering, Pattern 6 getStats fields, Pattern 7 error taxonomy)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-PATTERNS.md (Test Patterns section — MockRTCPeerConnection / mock getStats Map / mock getUserMedia techniques)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-UI-SPEC.md (Copywriting Contract — the 5 error types map to MediaError enum values)
  </read_first>
  <action>
    Create PeerManager.test.ts: vi.stubGlobal('RTCPeerConnection', MockRTCPeerConnection) where the mock exposes event
    hooks (onnegotiationneeded, oniceconnectionstatechange), setLocalDescription/setRemoteDescription/addIceCandidate
    spies, and a settable signalingState. Assert: (a) firing onnegotiationneeded calls setLocalDescription then emits a
    signal via the injected sendSignal callback; (b) an ice-candidate received while remoteDescription is null is buffered
    then drained (addIceCandidate called) after setRemoteDescription resolves; (c) impolite peer ignores a colliding offer
    (ignoreOffer path). Mock the callStore via vi.hoisted + vi.mock the same way wsClient.test mocks presenceStore.
    Create media.test.ts: mock navigator.mediaDevices.getUserMedia; assert error name mapping NotAllowedError→'permission-denied',
    NotFoundError→'no-device' (and audio-only fallback retried), NotReadableError→'device-busy', OverconstrainedError→
    'overconstrained' (fallback retried), SecurityError→'security-error'. Assert audio-only retry path returns mode 'audio-only'.
    Create stats.test.ts: mock getStats returning an RTCStatsReport (Map) with remote-inbound-rtp (roundTripTime, fractionLost),
    outbound-rtp (bytesSent, frameWidth/Height, codecId), transport→candidate-pair→local-candidate chain. Use vi.useFakeTimers;
    assert RTT parsed (×1000 to ms), bitrate delta computed across two polls, codec/resolution surfaced, ICE candidateType
    resolved to 'relay'/'srflx'/'host'.
    All three MUST fail now (PeerManager.ts / media.ts / stats.ts do not exist) — import errors are acceptable RED.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/webrtc/ --reporter=dot; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under frontend/src/webrtc/: PeerManager.test.ts, media.test.ts, stats.test.ts
    - PeerManager.test.ts contains a stubGlobal of RTCPeerConnection and asserts candidate buffering + drain
    - media.test.ts asserts all five DOMException name→MediaError mappings and the audio-only fallback path
    - stats.test.ts uses vi.useFakeTimers and asserts bitrate delta across two getStats polls and ICE candidateType resolution
    - npx vitest run src/webrtc/ exits non-zero (RED), proving the tests fail because production modules are missing
  </acceptance_criteria>
  <done>Three frontend test files exist and fail (production webrtc modules absent), establishing the GREEN targets for plans 03-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test-only plan) | This plan creates tests; no runtime trust boundary is crossed. Tests ENCODE the boundary assertions enforced by later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | signaling `from` field | mitigate | CallSignalingTest asserts a spoofed client `from` is overridden by the server-stamped principal (test encodes the Phase-2 server-owns-identity rule for call routing) |
| T-03-02 | Information Disclosure | TURN credential formula | mitigate | TurnControllerTest independently recomputes the HMAC-SHA1 ephemeral credential, locking the contract that no static secret is shipped |
| T-03-SC | Tampering | npm/maven installs | accept | No new packages installed in this plan; vite-plugin-mkcert (optional) is introduced in plan 05 where its legitimacy is gated |
</threat_model>

<verification>
- Backend: `cd backend && ./mvnw test -Dtest="CallSignalingTest,TurnControllerTest" -q` → fails (RED, expected).
- Frontend: `cd frontend && npx vitest run src/webrtc/` → fails (RED, expected).
- Manual: confirm each failure is a missing-production-code failure, not a test syntax/compile error in the test itself.
</verification>

<success_criteria>
- Five RED test files exist and are committed.
- Each test fails for the correct reason (production code absent).
- Contracts locked: signaling message shapes, server-stamped `from`, TURN HMAC formula, MediaError taxonomy, getStats field map.
</success_criteria>

<output>
Create `.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-01-SUMMARY.md` when done.
</output>
