---
phase: 3
slug: 1-1-p2p-call-core-nat-traversal
status: approved
reviewed_at: 2026-06-18
shadcn_initialized: false
preset: none
created: 2026-06-18
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: 1-1 P2P Call Core & NAT Traversal.
> Generated by gsd-ui-researcher.
>
> **Scope (new screens + augmented Home surface):**
> 1. A new `/call` route (React Router, ProtectedRoute) — the full in-call screen (D-05).
> 2. Pre-call self-view: mirrored local video preview + getUserMedia error states + audio-only fallback (D-06 / MEDIA-02 / MEDIA-05).
> 3. Call initiation from the Home online list: outgoing-call overlay, incoming-call card with Nhận/Từ chối, Hủy while calling (D-01).
> 4. In-call layout: remote video (main), local PiP self-view, hang-up control, quality indicator (RTT/packet loss), debug panel (hidden by default, toggled by button) (D-05 / STAB-03 / STAB-04).
> 5. Connection/ICE state surfacing: connecting → connected → reconnecting → failed, reusing Phase 2 ConnectionIndicator visual language.
>
> **Out of scope for this UI-SPEC (→ Phase 4):** full lifecycle polish, mute/cam-off toggles, device selection dropdowns, PiP drag, call duration timer, end-reason screens, ringtone, timeout→missed, busy, glare.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | **none** — plain vanilla CSS + inline `style={{}}` objects (matches shipped baseline, identical to Phase 2) |
| Preset | not applicable — `components.json` absent; shadcn not initialized |
| Component library | **none** — hand-rolled inline-styled React elements (no radix / base-ui / MUI) |
| Icon library | **none** — Unicode glyphs and CSS shapes only; no icon package |
| Font | inherited `var(--sans)` from `index.css`: `system-ui, 'Segoe UI', Roboto, sans-serif` |

**Baseline confirmation (codebase scan, 2026-06-18):**
- `frontend/package.json` — no Tailwind, shadcn, radix, lucide, or any component-library dependency.
- `frontend/src/pages/HomePage.tsx` — inline `style={{}}` objects throughout.
- `frontend/src/index.css` — CSS custom properties: `--accent`, `--text`, `--text-h`, `--border`, `--bg`, `--code-bg`, `--accent-bg`, `--accent-border`, `--social-bg`, `--shadow`, `--sans`. Dark mode via `prefers-color-scheme: dark`.
- `frontend/src/components/presence/` — Phase 2 components (`ConnectionIndicator`, `OnlineUserRow`, `OnlineUsersList`, `SessionKickNotice`, `StatusBadge`) follow this pattern exactly.

**Decision (per additional_context and CONTEXT.md D-05/D-06):** Phase 3 stays on the vanilla CSS / inline-style baseline. Reuse all `index.css` CSS variables so light/dark mode works for free. No new styling tooling introduced. New components live under `frontend/src/components/call/`.

---

## Spacing Scale

Inherits Phase 2 scale without exception. New call-specific usages noted in the Usage column.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap between status dot and label; debug panel row label-to-value gap |
| sm | 8px | Control button inner padding (vertical); gap between controls in the call toolbar |
| md | 16px | Call toolbar horizontal padding; MediaErrorNotice card inner padding; QualityIndicator internal padding |
| lg | 24px | CallPage safe-area padding from screen edge; IncomingCallCard padding |
| xl | 32px | Vertical gap between remote video and control bar; empty remote-video placeholder text top margin |
| 2xl | 48px | Not introduced in Phase 3 UI (reserved) |
| 3xl | 64px | Not introduced in Phase 3 UI (reserved) |

Exceptions:
- Self-view PiP: **160px × 120px** (fixed dimension; aspect ratio 4:3; overrides normal token grid — this is a media constraint, not layout spacing).
- Hang-up button diameter: **56px** (fixed circular touch target; exceeds `xl` for finger reachability on mobile demo).
- Control icon buttons (debug toggle, audio indicator): **44px × 44px** minimum touch target (WCAG 2.5.5 AAA guidance for touch).
- Remote video occupies all remaining viewport height above the control bar — no fixed token; uses `flex: 1` / `height: 100%` within the CallPage flex column.
- Video letterbox bars (black): `background: #000` — not a token, it is a media fill color (see Color).

---

## Typography

Inherits Phase 2 system exactly (400 regular + 600 semibold; no third weight).

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 28px | 600 | 1.2 | Caller/callee name on IncomingCallCard heading |
| Heading | 20px | 600 | 1.2 | "Đang gọi…" / "Cuộc gọi đến" state heading; self-view section label |
| Body | 16px | 400 | 1.5 | MediaErrorNotice message body; IncomingCallCard sub-text; pre-call instruction copy |
| Label | 14px | 600 | 1.4 | QualityIndicator values (RTT, loss); DebugPanel key-value rows; call control button labels (if shown); ICE state badge text |

**Monospace note:** DebugPanel raw values (codec string, bitrate number, resolution, candidate type) use `var(--mono)` at the **Label size (14px/400)** — only the font stack changes to monospace; **no new font size is introduced** (total sizes remain 4: 28/20/16/14).

**Font:** `var(--sans)` throughout. `var(--mono)` for DebugPanel data only. No web-font fetch.

---

## Color

All colours reference existing `index.css` CSS variables. Three new semantic additions for call-specific states (hang-up red, video-surface black, audio-only amber badge). Light/dark pairs declared for each.

| Role | Light value | Dark value | Usage |
|------|-------------|------------|-------|
| Dominant (60%) | `var(--bg)` `#fff` | `var(--bg)` `#16171d` | CallPage background behind video; toolbar background; DebugPanel surface |
| Secondary (30%) | `var(--code-bg)` `#f4f3ec` | `var(--code-bg)` `#1f2028` | IncomingCallCard card surface; DebugPanel panel surface; MediaErrorNotice card background |
| Accent (10%) | `var(--accent)` `#aa3bff` | `var(--accent)` `#c084fc` | ICE "connected" state dot; "Gọi" (initiate call) button outline/highlight; debug-toggle button active state — reserved list below |
| Destructive | `#dc2626` | `#f87171` | Hang-up button background; "Từ chối" (reject) button; ICE "failed" state indicator |

**Accent reserved for (explicit list — never applied broadly):**
- The ICE/WebRTC connection "connected" state dot inside `<QualityIndicator>` (mirrors Phase 2 ConnectionIndicator "Đã kết nối" treatment).
- The "Gọi" (initiate call) button on `<OnlineUserRow>` — a small accent-coloured text/icon button, not a full accent background fill.
- The debug-toggle button in its active/open state (accent border, `var(--accent-border)`).
- No other elements use accent.

**Call-specific semantic additions:**

| Semantic | Light | Dark | Usage |
|----------|-------|------|-------|
| Video surface | `#000000` | `#000000` | Background of `<video>` elements (letterbox fill; mode-independent) |
| Hang-up / destructive | `#dc2626` | `#f87171` | HangUpButton background; TừChoiButton background (same as Phase 2 destructive) |
| Audio-only badge bg | `#fef3c7` | `#1c1b0a` (approx) | AudioOnlyBadge background ("Chỉ âm thanh" badge on self-view / in-call) |
| Audio-only badge text | `#92400e` | `#fbbf24` | AudioOnlyBadge text (amber; matches Phase 2 IN_CALL amber palette) |
| ICE checking/connecting | `#6b7280` | `#9ca3af` | QualityIndicator state dot — grey, pulsing (matches Phase 2 "connecting" grey) |
| ICE connected | `#16a34a` or `var(--accent)` | `#4ade80` or `var(--accent)` | QualityIndicator state dot — green/accent |
| ICE failed | `#dc2626` | `#f87171` | QualityIndicator state dot — destructive red |
| HủyButton / neutral | `var(--border)` bg, `var(--text-h)` text | same | "Hủy" (cancel) button — not destructive, not accent; neutral |

**Contrast:** hang-up white icon on `#dc2626` ≈ 5.5:1 (AA). Audio-only badge `#92400e` on `#fef3c7` ≈ 6:1 (AA). Both meet WCAG 2.1 AA.

---

## Component Inventory

All hand-rolled, inline-styled React. New components under `frontend/src/components/call/`. Reuse Phase 2 components (`ConnectionIndicator`, `StatusBadge`) directly where noted.

### `<CallPage>` — route `/call`

The full-screen in-call view. Mounted via `<ProtectedRoute>`.

**Layout (flex column, full viewport height):**
```
┌─────────────────────────────────┐
│  [ICE state bar]  [Debug toggle]│  ← top bar, 44px tall, padding: 0 16px
├─────────────────────────────────┤
│                                 │
│       Remote video (flex:1)     │  ← <video> object-fit:cover, bg:#000
│                                 │
│   ┌──────────┐                  │
│   │Self-view │ (PiP, 160×120)  │  ← absolute, bottom-right corner of remote video
│   └──────────┘                  │
├─────────────────────────────────┤
│     [Hang up]                   │  ← control bar, padding: 16px, centered
└─────────────────────────────────┘
│  [DebugPanel] (hidden by default, renders below control bar when toggled)
```

States:
- **Connecting:** remote video is black (`#000`), ICE bar shows "Đang kết nối…" (grey dot).
- **Connected:** remote video shows remote stream, ICE bar shows "Đã kết nối" (green/accent dot) + `<QualityIndicator>` values.
- **Reconnecting:** remote video dims (`opacity: 0.6`), ICE bar shows "Đang kết nối lại…" (destructive dot).
- **Failed:** remote video black, ICE bar shows "Kết nối thất bại" (destructive dot), hang-up button still functional.
- **Audio-only:** remote video replaced by a centered avatar/icon placeholder + `<AudioOnlyBadge>`, local self-view shows no video.

### `<SelfViewPreview>` — pre-call screen (shown before `/call` route, or as a modal launched from Home)

Displayed when the user initiates a call (caller side), before the WebRTC connection is established. Also the state the callee sees while deciding to accept.

**Layout:**
- Centered card: `max-width: 480px`, `background: var(--code-bg)`, `border-radius: 8px`, `padding: 24px`, `var(--shadow)`.
- `<video autoPlay muted playsInline style={{ transform: 'scaleX(-1)', width: '100%', borderRadius: 4, background: '#000' }}` — mirrored self-view.
- Below video: status text ("Đang gọi cho {username}…" or "Đang chuẩn bị…"), then `<HủyButton>`.
- If getUserMedia failed: video area replaced by `<MediaErrorNotice>` card.
- If audio-only mode active: `<AudioOnlyBadge>` overlaid on the dark video area placeholder.

### `<IncomingCallCard>` — incoming call notification

Displayed on the callee's Home page when a `call-offer-received` message arrives. Rendered as a centered card overlay (not a full-page route change — the callee hasn't accepted yet).

**Layout (centered card overlay):**
- `position: fixed; inset: 0; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,0.5)`.
- Card: `background: var(--code-bg)`, `border-radius: 12px`, `padding: 24px`, `max-width: 360px`, `width: 100%`, `var(--shadow)`.
- Heading (28px/600): caller's username.
- Sub-text (16px/400): "Đang gọi video đến…"
- Two buttons side by side: `<NhậnButton>` (green, left) + `<TừChoiButton>` (destructive, right), `gap: 16px`.

States: only one state — the incoming ring state. Dismissed on accept (navigate to `/call`) or reject (card disappears, WS sends `call-reject`).

### `<NhậnButton>` / `<TừChoiButton>` / `<HủyButton>` / `<HangUpButton>`

Inline-styled `<button>` elements. Style specs:

| Button | Background | Text/Icon | Border-radius | Min width | Height |
|--------|-----------|-----------|--------------|-----------|--------|
| NhậnButton | `#16a34a` | `#fff`, "Nhận" | 8px | 120px | 44px |
| TừChoiButton | `#dc2626` | `#fff`, "Từ chối" | 8px | 120px | 44px |
| HủyButton | `var(--border)` | `var(--text-h)`, "Hủy" | 8px | 120px | 44px |
| HangUpButton | `#dc2626` | `#fff`, "●" (or ✕ Unicode) | 50% (circle) | 56px | 56px |

Font for all buttons: 16px/400 (NhậnButton/TừChoiButton/HủyButton) or 20px/600 (HangUpButton icon). All buttons: `cursor: pointer`, `:hover` lightens the background by ~10% (use CSS filter: brightness(1.1) or explicit lighter hex — do not introduce CSS classes, keep inline).

### `<QualityIndicator>`

Displayed in the CallPage top bar (right side of ICE state bar), visible while connected.

**Layout:** `display: flex; gap: 8px; align-items: center; font: 14px/1.4 var(--sans); font-weight: 600`.

**Contents:** `[ICE dot] [ICE state text]  [RTT: Xms]  [Loss: X%]`

- ICE dot: 8px coloured circle (`border-radius: 50%`), colour from ICE state palette above.
- ICE state text: 14px/600, colour matches dot.
- RTT/Loss numbers: 14px/400 (body weight), `var(--text)` colour.
- RTT thresholds → text colour overlay: ≤100ms `var(--text)`, 100–300ms `#d97706` (amber), >300ms `#dc2626` (destructive).
- Loss thresholds: ≤2% `var(--text)`, 2–10% `#d97706`, >10% `#dc2626`.
- Shows "—" for RTT/Loss when not yet available (first few seconds).

`role="status" aria-live="polite"` — screen-reader friendly updates (same pattern as Phase 2 `ConnectionIndicator`).

### `<DebugPanel>`

Hidden by default. Toggled by a button in the CallPage top bar (labelled "Debug" or "⚙" Unicode glyph).

**Toggle button:** `44px × 44px`, `background: transparent`, `border: 1px solid var(--border)`, `border-radius: 4px`. When panel is open: `border-color: var(--accent-border)`, `color: var(--accent)`.

**Panel layout:**
- Renders below the call control bar (not overlaid on video — preserves remote video visibility).
- `background: var(--code-bg)`, `border-top: 1px solid var(--border)`, `padding: 8px 16px`.
- Grid: 2-column label/value layout, `gap: 4px 16px`, rows:

| Label | Value example | Font |
|-------|---------------|------|
| Codec | `video/VP8` | `var(--mono)` 14px/400 |
| Bitrate | `842 kbps` | `var(--mono)` 14px/400 |
| Resolusi | `1280×720` | `var(--mono)` 14px/400 |
| ICE | `relay` | `var(--mono)` 14px/400, colour: relay→`#dc2626`, srflx→`#d97706`, host→`var(--text)` |
| RTT | `47 ms` | `var(--mono)` 14px/400 |
| Loss | `0.3%` | `var(--mono)` 14px/400 |

- Label text: 14px/600 `var(--text)`.
- Shows "—" for unavailable values.
- No close button — the same toggle button in the top bar closes it.

`aria-expanded` on the toggle button reflects panel state.

### `<MediaErrorNotice>`

Shown inside `<SelfViewPreview>` (or as a standalone notice on `/call` if the error occurs post-accept) when `getUserMedia` fails.

**Layout:** replaces the `<video>` element area.
- `background: var(--code-bg)`, `border-radius: 4px`, `padding: 16px`, `text-align: center`.
- Icon: Unicode glyph "⚠" (24px, `#d97706`).
- Heading (16px/600): error-specific short label (see Copywriting).
- Body (16px/400): actionable instruction (see Copywriting).
- If fallback path available: `<AudioOnlyFallbackButton>` — a text button "Tiếp tục với âm thanh" (14px/600, `var(--accent)` text, no background, no border).
- If no fallback available: no action button (user must fix externally and retry).

`role="alert"` so the error is announced immediately.

### `<AudioOnlyBadge>`

Displayed as an overlay on the dark video placeholder when running in audio-only mode (local or remote, or both).

- Amber pill: `background: #fef3c7` / `#1c1b0a` (dark), `color: #92400e` / `#fbbf24`, `padding: 4px 8px`, `border-radius: 999px`, `font: 14px/1 weight 600`.
- Text: "Chỉ âm thanh".
- Absolutely positioned: `top: 8px; left: 8px` inside the video container.

---

## Screens & States

### Screen 1: Home Page `/` — Augmented with Call Initiation

**What is added to the existing `<OnlineUserRow>`:** a small "Gọi" button rendered to the right of the `<StatusBadge>`, visible only when the row's user is `ONLINE` (not `IN_CALL`, not self).

- "Gọi" button: `font: 14px/1 weight 600`, `color: var(--accent)`, `background: transparent`, `border: 1px solid var(--accent-border)`, `border-radius: 4px`, `padding: 4px 8px`. Hover: `background: var(--accent-bg)`.
- Clicking "Gọi" triggers the call flow (see Interaction Patterns).
- While in an outgoing or incoming call: all other "Gọi" buttons are **disabled** (`opacity: 0.4`, `cursor: not-allowed`).

**`<IncomingCallCard>` placement:** fixed-position overlay over the entire Home page (see Component Inventory). Rendered by a Zustand `callStore` state subscription inside `HomePage` (or a top-level `<App>` component wrapper).

**Pre-call / outgoing-call state (caller side):** After clicking "Gọi", before the callee responds:
- `<SelfViewPreview>` card is rendered (modal-like, fixed-position overlay), showing mirrored local video.
- Heading: "Đang gọi cho {username}…".
- `<HủyButton>` below the video: "Hủy cuộc gọi".

### Screen 2: `/call` Route — In-Call

Four states (all within the same route, driven by `callStore` ICE connection state):

| State | Remote video | ICE bar | Controls |
|-------|-------------|---------|----------|
| Connecting | Black `#000` | "Đang kết nối…" (grey dot, pulse) | HangUpButton active |
| Connected | Remote stream | "Đã kết nối" + RTT/Loss (green/accent dot) | HangUpButton + DebugToggle |
| Reconnecting | Dimmed (`opacity:0.6`) | "Đang kết nối lại…" (destructive dot) | HangUpButton active |
| Failed | Black `#000` | "Kết nối thất bại" (destructive dot) | HangUpButton active |

**Audio-only mode (either side):** remote video area shows a centered grey avatar circle (64px diameter, `background: var(--border)`, `border-radius: 50%`) with the remote user's initials (28px/600, `var(--text-h)`) and an `<AudioOnlyBadge>` in the top-left corner of that area.

**Self-view PiP (local, in-call):** same mirrored video element (`transform: scaleX(-1)`), `160px × 120px`, `border-radius: 4px`, `border: 2px solid var(--bg)`, positioned `bottom: 8px; right: 8px` inside the remote video container (`position: absolute`). If local is audio-only: PiP area shows a small dark rectangle with "🎤" (microphone Unicode, 20px) centered.

### Screen 3: Error / Media Failure

Occurs at any point where `getUserMedia` fails (pre-call or post-accept). `<MediaErrorNotice>` replaces the video element. If in-call and the local stream suddenly fails: the notice overlays the self-view PiP area; the remote video is unaffected.

---

## Copywriting Contract

All copy in Vietnamese. No English copy in the UI.

### Call Initiation

| Element | Copy |
|---------|------|
| "Gọi" button on OnlineUserRow | Gọi |
| Pre-call heading (caller waiting) | Đang gọi cho {username}… |
| Pre-call sub-text | Đang chờ đối phương trả lời |
| Hủy button | Hủy cuộc gọi |
| Incoming call heading | {callerUsername} |
| Incoming call sub-text | Đang gọi video đến… |
| Nhận button | Nhận |
| Từ chối button | Từ chối |

### In-Call States

| Element | Copy |
|---------|------|
| ICE state — connecting | Đang kết nối… |
| ICE state — connected | Đã kết nối |
| ICE state — reconnecting | Đang kết nối lại… |
| ICE state — failed | Kết nối thất bại |
| Hang-up button (accessible label) | Kết thúc cuộc gọi |
| Debug toggle button (aria-label) | Bảng debug (ẩn/hiện) |
| Audio-only badge | Chỉ âm thanh |
| Remote avatar (when audio-only) | (initials of remote username) |

### getUserMedia Error States

| Error type | Short heading (16px/600) | Actionable body (16px/400) | Fallback button copy |
|------------|--------------------------|----------------------------|----------------------|
| permission-denied | Không có quyền truy cập camera/mic | Hãy kiểm tra quyền trình duyệt và tải lại trang. | (no fallback — must fix permissions) |
| no-device | Không tìm thấy camera | Không tìm thấy camera. Bạn có thể tiếp tục chỉ với âm thanh. | Tiếp tục với âm thanh |
| device-busy | Camera đang được dùng bởi ứng dụng khác | Hãy đóng ứng dụng đang dùng camera và thử lại. | (no fallback) |
| overconstrained | Camera không đáp ứng được yêu cầu | Tiếp tục với âm thanh. | Tiếp tục với âm thanh |
| security-error | Trang cần HTTPS để truy cập camera | Truy cập ứng dụng qua địa chỉ HTTPS. | (no fallback) |
| unknown | Không thể truy cập thiết bị | Có lỗi không xác định. Vui lòng tải lại trang. | (no fallback) |

### Empty / Error States

| Element | Copy |
|---------|------|
| Remote video loading placeholder (black screen) | (no text — black is intentional) |
| Remote video audio-only placeholder heading | (initials avatar — no text label) |
| Call failed — body (under "Kết nối thất bại") | Không thể thiết lập kết nối ngang hàng. Hãy thử gọi lại. |
| Post-reject (back on Home — no toast needed) | (no copy — call card simply disappears; no explicit confirmation needed for reject/cancel at Phase 3 scope) |

### Destructive Actions

| Action | Confirmation approach |
|--------|-----------------------|
| Hủy cuộc gọi (cancel outgoing) | **No confirmation dialog** — the call hasn't connected; Hủy is immediately reversible by calling again. Single tap fires `call-cancel`. |
| Kết thúc cuộc gọi (hang up, in-call) | **No confirmation dialog** — ending a call is unambiguous and fast; a dialog would block a user who needs to end urgently. Single tap on HangUpButton fires `hang-up`. |
| Từ chối cuộc gọi (reject incoming) | **No confirmation dialog** — rejecting is immediate and the callee can call back. Single tap fires `call-reject`. |

---

## Interaction Patterns

### Pattern 1: Call Initiation Flow (Caller)

1. User clicks "Gọi" on an `<OnlineUserRow>`.
2. `callStore` sets `callState = 'outgoing'`, `remoteUserId = targetId`.
3. `acquireMedia()` is called: tries `getUserMedia({ audio, video })`.
   - On success: `callStore` sets `localStream` reference (outside Zustand — PeerManager holds it); `callStore` sets `mediaMode = 'av'`.
   - On `NotFoundError`/`OverconstrainedError`: try audio-only fallback; if OK, `callStore` sets `mediaMode = 'audio-only'`, shows `<AudioOnlyBadge>`.
   - On other error: render `<MediaErrorNotice>`, abort call flow.
4. `<SelfViewPreview>` overlay renders with the local stream playing (mirrored).
5. WS sends `{ type: 'call-offer', to: targetId, callId }`.
6. UI shows "Đang gọi cho {username}…" + `<HủyButton>`.
7. If callee accepts (`call-accept` arrives): navigate to `/call`; `PeerManager` starts perfect negotiation.
8. If callee rejects (`call-reject` arrives): dismiss overlay, `callStore` resets, back to normal Home.
9. If Hủy is tapped: WS sends `{ type: 'call-cancel', to: targetId, callId }`; dismiss overlay, `callStore` resets.

### Pattern 2: Incoming Call Flow (Callee)

1. WS receives `call-offer-received` → `callStore` sets `callState = 'incoming'`, `remoteUserId = callerId`.
2. `<IncomingCallCard>` renders as fixed overlay on Home.
3. If "Nhận" tapped:
   a. `acquireMedia()` called (same error handling as Pattern 1).
   b. WS sends `{ type: 'call-accept', to: callerId, callId }`.
   c. Navigate to `/call`.
   d. `PeerManager` initialises with `polite = true` (callee is always polite in Phase 3).
4. If "Từ chối" tapped: WS sends `{ type: 'call-reject', to: callerId, callId }`; `callStore` resets; card dismisses.

### Pattern 3: getUserMedia Error + Audio-Only Fallback

1. `acquireMedia()` catches `NotFoundError` or `OverconstrainedError`.
2. Retries `getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true } })`.
   - Success: continue call flow in audio-only mode; render `<AudioOnlyBadge>` in self-view; `<MediaErrorNotice>` with "Tiếp tục với âm thanh" fallback button is NOT shown (fallback already taken automatically).
   - Failure: render `<MediaErrorNotice>` with the appropriate error copy; no call attempt proceeds.
3. For `NotAllowedError`, `NotReadableError`, `SecurityError`, `unknown`: do NOT attempt fallback; render `<MediaErrorNotice>` immediately with the correct message from the Copywriting Contract.

### Pattern 4: Debug Panel Toggle

1. Debug toggle button in the CallPage top bar (always visible in-call, `aria-expanded="false"` initially).
2. Tap/click: `DebugPanel` renders below the control bar; button `aria-expanded="true"`, border changes to `var(--accent-border)`.
3. Tap/click again: `DebugPanel` hidden; button returns to default style; `aria-expanded="false"`.
4. `DebugPanel` polls `getStats()` every **1000ms** while visible; stops polling when hidden (no wasted work).
5. The debug state (open/closed) is local component state (`useState`) — not in `callStore` (it is a developer tool, not application state).

### Pattern 5: Hang Up (either side)

1. HangUpButton tapped in-call (or `hang-up` message received from remote).
2. `PeerManager.close()` called — closes `RTCPeerConnection`, stops local media tracks.
3. WS sends `{ type: 'hang-up', to: remoteUserId, callId }` (only if locally initiated).
4. Navigate back to `/` (Home).
5. `callStore` resets to idle state.
6. No end-reason screen in Phase 3 (→ Phase 4).

### Pattern 6: ICE / Connection State Surfacing

`RTCPeerConnection` fires `oniceconnectionstatechange`. State → UI mapping:

| `iceConnectionState` | `callStore` state | `QualityIndicator` display |
|---------------------|-------------------|---------------------------|
| `'new'` / `'checking'` | `'connecting'` | "Đang kết nối…" grey pulsing dot |
| `'connected'` / `'completed'` | `'connected'` | "Đã kết nối" green/accent dot + RTT/Loss |
| `'disconnected'` | `'reconnecting'` | "Đang kết nối lại…" destructive dot; remote video dims |
| `'failed'` | `'failed'` | "Kết nối thất bại" destructive dot |
| `'closed'` | `'idle'` | (navigate away — this state is never displayed) |

**Important:** `callStore` only holds the derived string state (`'connecting' | 'connected' | 'reconnecting' | 'failed' | 'idle'`), not the `RTCPeerConnection` object itself (CLAUDE.md mandate: non-serializable objects stay in PeerManager, not Zustand).

---

## Accessibility Baseline

- `<QualityIndicator>`: `role="status" aria-live="polite"` — ICE state changes announced (mirrors Phase 2 `ConnectionIndicator`).
- `<MediaErrorNotice>`: `role="alert"` — error is announced immediately on render.
- `<IncomingCallCard>` overlay: `role="dialog" aria-modal="true" aria-labelledby="incoming-call-heading"`. Focus is trapped inside the card (Nhận / Từ chối are the only interactive elements). On dismiss, focus returns to the element that had focus before the card appeared.
- `<HangUpButton>`: `aria-label="Kết thúc cuộc gọi"` (icon-only button must have accessible label).
- `<DebugToggleButton>`: `aria-label="Bảng debug (ẩn/hiện)" aria-expanded={open}`.
- `<NhậnButton>`: `aria-label="Nhận cuộc gọi"`. `<TừChoiButton>`: `aria-label="Từ chối cuộc gọi"`.
- Self-view `<video>`: `aria-label="Camera của bạn" aria-hidden="false"`. Remote `<video>`: `aria-label="Camera của {remoteUsername}"`.
- `<AudioOnlyBadge>`: text "Chỉ âm thanh" — visible and in the DOM; not `aria-hidden`.
- Status conveyed by colour + text, never colour alone (ICE dot = decorative; text carries meaning).
- Colour contrast: all new colour pairings meet WCAG 2.1 AA (see Color section contrast notes).
- Keyboard: all call buttons reachable via Tab; HangUpButton is the last focusable element in the call control bar (natural Tab order).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized (`components.json` absent) |
| third-party | none | not applicable — no third-party registries declared |

No registries used. All elements are hand-rolled vanilla-CSS / inline-styled React. No new npm dependency is introduced by this UI contract (the only new tooling in Phase 3 is `mkcert` — a CLI tool, not an npm package — and optionally `vite-plugin-mkcert` npm package, which is a dev-only build tool with no UI surface).

---

## Design Decisions Pre-Populated From Upstream Artifacts

| Decision | Source |
|----------|--------|
| Vanilla CSS + inline styles, no Tailwind | CONTEXT.md additional_context; Phase 2 UI-SPEC shipped baseline |
| `/call` as dedicated route (not overlay on Home) | CONTEXT.md D-05 |
| Debug panel hidden, toggle-only | CONTEXT.md D-05 |
| Self-view mirrored (`scaleX(-1)`) | CONTEXT.md D-06; RESEARCH.md Pattern 7 (Pitfall 6) |
| getUserMedia error taxonomy (5 types) | RESEARCH.md Pattern 7 |
| Audio-only fallback on `NotFoundError`/`OverconstrainedError` | CONTEXT.md D-06; RESEARCH.md Pattern 7 |
| RTT/loss from `remote-inbound-rtp`; bitrate from `outbound-rtp` delta | RESEARCH.md Pattern 6 |
| ICE candidate type via transport→candidate-pair→local-candidate | RESEARCH.md Pattern 6 |
| `getStats` poll cadence 1000ms | RESEARCH.md Pattern 6 (Claude's discretion, widely-used practice) |
| Callee is always polite in Phase 3 | RESEARCH.md Pattern 1 (polite/impolite: callee polite, simpler for Phase 3) |
| accept/reject/cancel: no confirmation dialogs | Rationale in Copywriting Contract above; calls are fast, reversible by redialling |
| `callStore` holds only serializable derived state | CLAUDE.md mandate; CONTEXT.md Claude's Discretion |
| Reuse `var(--accent)` / `var(--border)` / destructive palette | Phase 2 UI-SPEC color section |
| `var(--mono)` for DebugPanel data | Existing `index.css` font stack |
| Vietnamese copy throughout | Phase 2 UI-SPEC copywriting pattern; CLAUDE.md |
| 1000ms getStats poll stops when DebugPanel is hidden | Claude's discretion — performance |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
