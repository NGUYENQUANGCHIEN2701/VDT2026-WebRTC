---
phase: 03-1-1-p2p-call-core-nat-traversal
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-03"]
files_modified:
  - frontend/src/webrtc/stats.ts
  - frontend/src/components/call/QualityIndicator.tsx
  - frontend/src/components/call/DebugPanel.tsx
  - frontend/src/pages/CallPage.tsx
autonomous: true
requirements: [STAB-03, STAB-04]
must_haves:
  truths:
    - "In-call user sees a network quality indicator showing RTT and packet loss derived from getStats"
    - "A debug panel (hidden by default, toggled by a button) shows codec, bitrate, resolution, and ICE candidate type (host/srflx/relay)"
    - "getStats polling runs at 1000ms while the debug panel is visible and stops when hidden"
  artifacts:
    - path: "frontend/src/webrtc/stats.ts"
      provides: "getStats polling + parsing (RTT/loss/bitrate/codec/resolution/ICE type)"
      contains: "remote-inbound-rtp"
    - path: "frontend/src/components/call/QualityIndicator.tsx"
      provides: "RTT/packet-loss visual indicator (role=status)"
      contains: "aria-live"
    - path: "frontend/src/components/call/DebugPanel.tsx"
      provides: "Togglable codec/bitrate/resolution/ICE-type panel"
  key_links:
    - from: "DebugPanel.tsx"
      to: "stats.ts pollStats"
      via: "1000ms interval while visible"
      pattern: "1000"
    - from: "stats.ts"
      to: "RTCPeerConnection.getStats"
      via: "transport→candidate-pair→local-candidate ICE type resolution"
      pattern: "candidateType"
    - from: "CallPage.tsx"
      to: "QualityIndicator + DebugPanel"
      via: "top-bar mount + debug toggle useState"
      pattern: "QualityIndicator"
---

<objective>
Quality + diagnostics vertical slice. Surface connection quality (STAB-03) and a developer debug
panel (STAB-04) inside the in-call screen built in plan 03. RTT and packet loss come from the
remote-inbound-rtp getStats report; codec/bitrate/resolution from outbound-rtp; ICE candidate type
(host/srflx/relay) from the transport→candidate-pair→local-candidate chain. The debug panel is hidden
by default and toggled by a button (D-05); polling runs at 1000ms only while the panel is visible.

The ICE candidate type readout is also the proof surface for the TURN forced-relay test in plan 05
(it must read "relay" when iceTransportPolicy:'relay' is active).

Purpose: Make call quality visible (STAB-03) and expose technical stats for debugging + the TURN
relay proof (STAB-04).
Output: stats.test.ts goes GREEN; the in-call screen shows the quality indicator and a togglable debug panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-RESEARCH.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-PATTERNS.md
@.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stats.ts — getStats polling + parsing</name>
  <files>frontend/src/webrtc/stats.ts</files>
  <read_first>
    - frontend/src/webrtc/stats.test.ts (GREEN target — RTT/bitrate/codec/resolution/ICE-type parsing assertions; mock RTCStatsReport Map)
    - frontend/src/webrtc/PeerManager.ts (the RTCPeerConnection source for getStats; how the active pc is exposed)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-RESEARCH.md (Pattern 6 — report types/fields table, bitrate delta formula, ICE type resolution steps, 1000ms cadence)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-PATTERNS.md (stats.ts no-analog note — build from RESEARCH Pattern 6)
  </read_first>
  <behavior>
    - pollStats parses RTT (ms) from remote-inbound-rtp.roundTripTime ×1000 and fractionLost
    - bitrate (kbps) computed as a delta of outbound-rtp.bytesSent across consecutive polls (×8 / Δtime)
    - resolution from outbound-rtp.frameWidth×frameHeight; codec from outbound-rtp.codecId → codec report mimeType
    - ICE candidate type resolved via transport.selectedCandidatePairId → candidate-pair.localCandidateId → local-candidate.candidateType ('host'|'srflx'|'relay')
    - unavailable fields return undefined/null (UI shows "—")
  </behavior>
  <action>
    Create webrtc/stats.ts exporting a CallStats type (rttMs, fractionLost, bitrateKbps, frameWidth, frameHeight, codec, iceCandidateType)
    and a pollStats(pc) function plus a startStatsPolling(pc, onStats, intervalMs=1000) helper returning a stop() that clears the interval.
    Implement parsing exactly per RESEARCH Pattern 6: iterate the RTCStatsReport; remote-inbound-rtp → rttMs (roundTripTime×1000) + fractionLost;
    outbound-rtp(video) → bitrate delta (track prevBytesSent/prevTimestamp in module/closure scope) + frameWidth/Height + codecId; resolve codec
    mimeType via report lookup of codecId; resolve ICE type via transport→candidate-pair→local-candidate chain. Keep the polling pure and timer-based
    so DebugPanel can start/stop it; do not put stats in Zustand (component-local is fine — it is a dev tool).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/webrtc/stats.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - stats.test.ts PASSES (RED test from plan 01 now GREEN)
    - stats.ts reads RTT from a 'remote-inbound-rtp' report and multiplies roundTripTime by 1000
    - bitrate is computed as a delta across two polls (test advances fake timers and asserts a non-zero kbps)
    - ICE candidate type is resolved through the transport→candidate-pair→local-candidate chain (contains "candidateType")
    - startStatsPolling returns a stop() that clears the interval (no leaked timers asserted by the test)
  </acceptance_criteria>
  <done>stats.ts parses all STAB-03/04 metrics from getStats and supports start/stop polling; Wave 0 stats test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: QualityIndicator + DebugPanel components wired into CallPage</name>
  <files>frontend/src/components/call/QualityIndicator.tsx, frontend/src/components/call/DebugPanel.tsx, frontend/src/pages/CallPage.tsx</files>
  <read_first>
    - frontend/src/components/presence/ConnectionIndicator.tsx (canonical visual twin: role=status aria-live, 8px dot + colored text, Record<state,{color,text}> map)
    - frontend/src/pages/CallPage.tsx (top bar with reserved QualityIndicator/DebugToggle slot from plan 03; ICE state already driven by callStore)
    - frontend/src/webrtc/stats.ts (created Task 1 — pollStats/startStatsPolling + CallStats type)
    - frontend/src/store/callStore.ts (callState drives ICE dot color/text)
    - .planning/phases/03-1-1-p2p-call-core-nat-traversal/03-UI-SPEC.md (QualityIndicator + DebugPanel Component Inventory; Interaction Pattern 4 toggle + 1000ms poll stop-when-hidden; thresholds for RTT/loss colors; DebugPanel grid rows + var(--mono); Accessibility Baseline aria-expanded)
  </read_first>
  <action>
    Build QualityIndicator.tsx mirroring ConnectionIndicator (role="status" aria-live="polite", inline style, var(--*)): ICE dot + ICE
    state text from callStore.callState (grey connecting / green-accent connected / destructive reconnecting / destructive failed per UI-SPEC
    palette) plus RTT and Loss readouts with the threshold color overlays (RTT ≤100 text / 100-300 amber / >300 destructive; Loss ≤2 text /
    2-10 amber / >10 destructive); show "—" until available. Build DebugPanel.tsx: a 44×44 toggle button (aria-expanded, aria-label "Bảng debug
    (ẩn/hiện)", accent border when open) and the panel rendered below the control bar with the 2-column var(--mono) 14px grid rows Codec/Bitrate/
    Resolusi/ICE/RTT/Loss (ICE value color: relay→#dc2626, srflx→#d97706, host→var(--text)); "—" for unavailable. DebugPanel owns local useState
    open flag and starts startStatsPolling(pc, setStats, 1000) on open / calls stop() on close+unmount (no polling while hidden). Wire both into
    CallPage's top bar (QualityIndicator visible while connected; DebugToggle always visible in-call) — feed both from the active PeerManager's
    RTCPeerConnection. Debug open/closed state stays component-local, NOT in callStore (UI-SPEC Interaction Pattern 4.5).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes; `npm run build` succeeds; full `npx vitest run` stays green
    - QualityIndicator has role="status" aria-live="polite" and applies the RTT/loss threshold color overlays from UI-SPEC
    - DebugPanel toggle button has aria-expanded reflecting state and aria-label "Bảng debug (ẩn/hiện)"; panel uses var(--mono) for data values
    - DebugPanel ICE-type value is colored relay→#dc2626 / srflx→#d97706 / host→var(--text)
    - getStats polling starts only when the panel is open and stops on close/unmount (grep: startStatsPolling called in an effect with a cleanup)
    - CallPage top bar mounts QualityIndicator (when connected) and the DebugToggle; all copy Vietnamese, all styling inline var(--*)
  </acceptance_criteria>
  <done>The in-call screen shows a live RTT/loss quality indicator and a togglable debug panel exposing codec/bitrate/resolution/ICE type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTCPeerConnection.getStats → UI | Read-only diagnostic data; no user input crosses; no privileged action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | debug panel exposing internal stats | accept | Stats are local to the user's own session (codec/bitrate/ICE type); no other user's data is shown; panel hidden by default |
| T-03-11 | Denial of Service | runaway getStats polling | mitigate | Polling fixed at 1000ms and stops when the panel is hidden / unmounts (cleanup), bounding CPU cost |
| T-03-SC | Tampering | npm installs | accept | No new npm dependencies (native getStats only) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/webrtc/stats.test.ts` → GREEN.
- `cd frontend && npx vitest run && npx tsc --noEmit && npm run build` → all pass.
- Manual (covered by plan 05's 2-device/forced-relay checkpoint): debug panel ICE value reads host/srflx on STUN and relay under forced-relay.
</verification>

<success_criteria>
- In-call quality indicator shows RTT + packet loss from getStats.
- Debug panel (hidden by default, toggle button) shows codec, bitrate, resolution, ICE candidate type.
- Polling runs at 1000ms only while the panel is visible; Wave 0 stats test GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-1-1-p2p-call-core-nat-traversal/03-04-SUMMARY.md` when done.
</output>
