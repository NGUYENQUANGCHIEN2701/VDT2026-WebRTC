---
phase: 06-horizontal-scaling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceCallTest.java
  - backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java
  - backend/src/main/java/com/vdt/webrtc/presence/PresenceSweeper.java
autonomous: true
requirements:
  - SCAL-01
  - SCAL-02
must_haves:
  truths:
    - "CrossInstanceCallTest exists and fails (RED) with at least 3 test methods: crossInstance_callInvite_reachesCallee, crossInstance_presence_isConsistent, crossInstance_inCallStatus_isVisible (D-06: two Spring contexts + Testcontainers Redis)"
    - "WsTestSupport.drainState() autowires PresenceService (interface) not LocalPresenceService (concrete type)"
    - "PresenceSweeper constructor takes (StringRedisTemplate) only — no PresenceWebSocketHandler, no LocalPresenceService"
    - "All existing WsTestSupport-based tests still compile after the PresenceSweeper/WsTestSupport changes"
  artifacts:
    - path: "backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceCallTest.java"
      provides: "Cross-instance integration test scaffold — two SpringApplicationBuilder contexts sharing Testcontainers Redis, Postgres, RabbitMQ"
    - path: "backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java"
      provides: "Presence drain using PresenceService interface; CollectingHandler stays in place"
    - path: "backend/src/main/java/com/vdt/webrtc/presence/PresenceSweeper.java"
      provides: "Sweeper refactored to use StringRedisTemplate + publish presence-events; LocalPresenceService dependency removed"
  key_links:
    - from: "CrossInstanceCallTest"
      to: "SpringApplicationBuilder contexts"
      via: "Manual GenericContainer start + property injection (NOT @ServiceConnection)"
      pattern: "SpringApplicationBuilder.*VdtWebrtcApplication.*run"
    - from: "WsTestSupport.drainState"
      to: "PresenceService"
      via: "Autowired PresenceService (interface)"
      pattern: "protected PresenceService presence"
    - from: "PresenceSweeper.sweep"
      to: "StringRedisTemplate"
      via: "SMEMBERS online-users + hasKey presence:{userId}"
      pattern: "redis.opsForSet\\(\\).members"
---

## Phase Goal

**As a** backend developer verifying the scaling milestone, **I want to** have RED integration tests
that prove cross-instance signaling works before writing a line of Redis pub/sub code, **so that**
the keystone tests drive implementation and any regression is caught immediately.

<objective>
Wave 0 (RED state): create the cross-instance integration test scaffold, refactor WsTestSupport
to work with the upcoming Redis-backed PresenceService, and rewrite PresenceSweeper to take
StringRedisTemplate directly instead of LocalPresenceService.

Purpose: TDD wave. Tests are written first and MUST FAIL. The PresenceSweeper + WsTestSupport
changes are necessary pre-conditions for the Redis impl swap in Plan 02 — they eliminate the
two concrete-type injection points that would cause NoSuchBeanDefinitionException when
LocalPresenceService loses its @Service annotation.

The CAS blocker is RESOLVED per research: no Lua/WATCH-MULTI needed for Phase 6 keys. All new
Redis mutations (route map, presence TTL, online-users SET) are safe without CAS because
one-session-per-user makes route-map writes single-writer per key, TTL operations are idempotent,
and call-state CAS is already handled by Phase 4 Lua scripts. Executors MUST NOT add CAS work.

Output: CrossInstanceCallTest.java (RED), updated WsTestSupport.java, updated PresenceSweeper.java
</objective>

<execution_context>
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/workflows/execute-plan.md
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/ROADMAP.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/06-horizontal-scaling/06-CONTEXT.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/06-horizontal-scaling/06-RESEARCH.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/06-horizontal-scaling/06-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Refactor PresenceSweeper and WsTestSupport to remove LocalPresenceService concrete dependency</name>
  <files>
    backend/src/main/java/com/vdt/webrtc/presence/PresenceSweeper.java,
    backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java
  </files>

  <read_first>
    - backend/src/main/java/com/vdt/webrtc/presence/PresenceSweeper.java (file being modified — the current concrete-type injection)
    - backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java (file being modified — LocalPresenceService field on line 37)
    - backend/src/main/java/com/vdt/webrtc/presence/LocalPresenceService.java (analog — behavior being replaced; note evictStaleBefore method)
    - backend/src/main/java/com/vdt/webrtc/presence/PresenceService.java (the interface — WsTestSupport changes to this type)
    - backend/src/main/java/com/vdt/webrtc/call/CallStateRepository.java (analog for StringRedisTemplate usage pattern: opsForValue, opsForSet, hasKey)
    - .planning/phases/06-horizontal-scaling/06-PATTERNS.md (PresenceSweeper section — exact new sweep() body using SMEMBERS + hasKey)
  </read_first>

  <behavior>
    PresenceSweeper after change:
    - Constructor takes (StringRedisTemplate redis) only — LocalPresenceService removed, PresenceWebSocketHandler removed
    - sweep() calls redis.opsForSet().members("online-users") to enumerate current members
    - For each member where redis.hasKey("presence:" + userId) returns false, calls redis.opsForSet().remove("online-users", userId) and collects userId into evicted list
    - If evicted is non-empty, logs evicted list and calls redis.convertAndSend("presence-events", "changed") — no handler reference anywhere in the class (broadcast is triggered by the presence-events listener installed in Plan 02)
    - TTL_MS field is removed (not needed; TTL is enforced by Redis key expiry, not by timestamp comparison)

    WsTestSupport after change:
    - Line 37: protected LocalPresenceService presence changes to protected PresenceService presence
    - Import of LocalPresenceService removed; import of PresenceService added
    - drainState() logic unchanged (presence.snapshot().isEmpty() still compiles against PresenceService interface)
    - All methods in WsTestSupport that relied on LocalPresenceService-specific methods (e.g., evictStaleBefore) must be removed or rewritten — currently none are used outside sweep/drain; confirm via grep before removing
  </behavior>

  <action>
    Rewrite PresenceSweeper (per PATTERNS.md PresenceSweeper section):
    - Remove LocalPresenceService field and TTL_MS constant
    - Add StringRedisTemplate field (constructor injection — same pattern as CallStateRepository.java lines 13-15)
    - New sweep() body: SMEMBERS online-users → filter where hasKey("presence:{userId}") is false → SREM each → if any evicted: log and redis.convertAndSend("presence-events", "changed")
    - Keep @Scheduled(fixedDelay = 15_000) and @Component and @Slf4j unchanged
    - PresenceWebSocketHandler field: remove it entirely. The sweep() body publishes to presence-events via redis.convertAndSend; Plan 02's PresenceEventListener (a separate @Component) handles calling broadcastSnapshot(). PresenceSweeper has zero PresenceWebSocketHandler dependency.

    Update WsTestSupport:
    - Change field type from LocalPresenceService to PresenceService (per PATTERNS.md WsTestSupport section)
    - Fix import accordingly
    - The drainState await condition presence.snapshot().isEmpty() is unchanged

    Compiler check: ./mvnw -pl backend compile -q must succeed after these changes.
  </action>

  <verify>
    <automated>cd backend && ./mvnw -pl . compile -q 2>&1 | tail -5</automated>
  </verify>

  <done>
    - PresenceSweeper.java: constructor takes (StringRedisTemplate) only — no PresenceWebSocketHandler field, no LocalPresenceService reference; sweep() body uses redis.opsForSet().members("online-users") and redis.convertAndSend("presence-events", "changed")
    - WsTestSupport.java: field is "protected PresenceService presence"; no LocalPresenceService import
    - ./mvnw compile exits 0 (no compilation errors introduced)
  </done>
</task>

<task type="tdd">
  <name>Task 2: Write CrossInstanceCallTest RED scaffold (two Spring contexts, shared Testcontainers Redis)</name>
  <files>
    backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceCallTest.java
  </files>

  <read_first>
    - backend/src/test/java/com/vdt/webrtc/ws/WsTestSupport.java (CollectingHandler inner class lines 85-126 — copy into CrossInstanceCallTest as static inner class)
    - backend/src/test/java/com/vdt/webrtc/ws/CallLifecycleTest.java (StandardWebSocketClient connect pattern lines 40-56; flushRedis pattern lines 24-26; awaitMatching assert pattern lines 45-49)
    - backend/src/test/java/com/vdt/webrtc/TestcontainersConfiguration.java (container image names: redis:7-alpine, postgres:17-alpine, rabbitmq:4.1-management)
    - backend/src/main/java/com/vdt/webrtc/config/JwtService.java (generateToken signature — used to mint tokens from ctx1.getBean(JwtService.class))
    - .planning/phases/06-horizontal-scaling/06-RESEARCH.md (Pattern 9 — cross-instance test skeleton with SpringApplicationBuilder, GenericContainer manual start, property injection)
    - .planning/phases/06-horizontal-scaling/06-PATTERNS.md (CrossInstanceCallTest section — two-context bootstrap, JWT minting, flushRedis teardown patterns)
  </read_first>

  <behavior>
    CrossInstanceCallTest is NOT a @SpringBootTest. It manages two ApplicationContexts manually.

    Class setup (static @BeforeAll):
    - Static GenericContainer<?> redis (redis:7-alpine), PostgreSQLContainer<?> postgres (postgres:17-alpine), RabbitMQContainer rabbitmq (rabbitmq:4.1-management) — all started in @BeforeAll
    - ctx1 = SpringApplicationBuilder(VdtWebrtcApplication.class) with properties: redis host/port, postgres JDBC/user/pass, rabbitmq host/port/user/pass, app.instance-id=inst1, server.port=0, call.ring-timeout-seconds=5
    - ctx2 = same properties except app.instance-id=inst2, server.port=0
    - Both contexts run(); stored in static ConfigurableApplicationContext fields

    Instance teardown (@AfterAll):
    - ctx1.close(), ctx2.close(), redis.stop(), postgres.stop(), rabbitmq.stop()

    Per-test teardown (@BeforeEach):
    - Flush Redis via ctx1.getBean(StringRedisTemplate.class).getConnectionFactory().getConnection().serverCommands().flushAll()

    Static inner class CollectingHandler (copied from WsTestSupport):
    - Exact copy of WsTestSupport.CollectingHandler: BlockingQueue<String> messages, handleTextMessage adds to queue, awaitMessage polls, awaitMatching polls with deadline loop and predicate

    Test 1 — crossInstance_callInvite_reachesCallee (covers SCAL-01):
    - Mint token for "alice" from ctx1.getBean(JwtService.class); mint token for "bob" from ctx1 (same JWT secret)
    - Connect Alice to ctx1 port, Bob to ctx2 port via StandardWebSocketClient
    - Alice sends call-invite to bob
    - Assert: hBob.awaitMatching(f -> f.contains("call-state-changed") && f.contains("\"state\":\"ringing\""), 5000) is not null
    - Assert: frame contains "callerId":"alice"
    - This test MUST FAIL in RED state because RedisMessageRouter does not exist yet

    Test 2 — crossInstance_presence_isConsistent (covers SCAL-02):
    - Connect Alice to ctx1; connect Bob to ctx2
    - After short delay for presence fan-out to propagate, check Bob's handler received a presence-snapshot frame containing "alice"
    - Assert: hBob.awaitMatching(f -> f.contains("presence-snapshot") && f.contains("alice"), 3000) is not null
    - MUST FAIL in RED (LocalPresenceService is still a real bean and no presence-events fan-out exists yet across instances)

    Test 3 — crossInstance_inCallStatus_isVisible (covers SCAL-02 D-04 busy derived from user-call:{userId}):
    - Alice on ctx1 calls Bob on ctx2; Bob accepts
    - A third user Carol connects to ctx1
    - Carol's presence-snapshot should show Bob as IN_CALL
    - Assert: hCarol receives snapshot with bob user showing "IN_CALL" status
    - MUST FAIL in RED

    All three tests are @Test methods. They compile but fail because the Redis routing/presence impls do not exist yet.
  </behavior>

  <action>
    Create backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceCallTest.java with:
    - Package: com.vdt.webrtc.ws
    - Required imports: SpringApplicationBuilder, ConfigurableApplicationContext, GenericContainer (testcontainers), PostgreSQLContainer, RabbitMQContainer, StandardWebSocketClient, JwtService, StringRedisTemplate, VdtWebrtcApplication, JUnit 5 annotations, AssertJ assertThat
    - Static container fields and static context fields (ctx1, ctx2)
    - @BeforeAll startContexts: start all three containers, build SpringApplicationBuilder for ctx1 with properties listed in behavior, build ctx2 with different instance-id
    - @AfterAll stop: close contexts, stop containers
    - @BeforeEach flushRedis: one flushAll via ctx1 StringRedisTemplate
    - Static inner class CollectingHandler copied verbatim from WsTestSupport.CollectingHandler (same BlockingQueue pattern, awaitMessage, awaitMatching)
    - Three @Test methods as described in behavior section

    RabbitMQ property injection pattern: get host/port/user/pass from RabbitMQContainer.getHost(), getMappedPort(5672), getAdminUsername(), getAdminPassword(). Spring property keys: spring.rabbitmq.host, spring.rabbitmq.port, spring.rabbitmq.username, spring.rabbitmq.password.
    Postgres property injection: spring.datasource.url = postgres.getJdbcUrl(), spring.datasource.username = postgres.getUsername(), spring.datasource.password = postgres.getPassword().
    Redis property injection: spring.data.redis.host, spring.data.redis.port = redis.getMappedPort(6379).

    Do NOT use @ServiceConnection here — that requires @SpringBootTest's ApplicationContext lifecycle.

    The test must compile and all three @Test methods must fail when run (RED state is required for TDD).
  </action>

  <verify>
    <automated>cd backend && ./mvnw -pl . -Dtest=CrossInstanceCallTest test -DfailIfNoTests=false 2>&1 | grep -E "(BUILD|FAIL|ERROR|Tests run:[[:space:]]*[1-9])" | tail -10</automated>
  </verify>

  <done>
    - CrossInstanceCallTest.java compiles without errors
    - All 3 test methods exist: crossInstance_callInvite_reachesCallee, crossInstance_presence_isConsistent, crossInstance_inCallStatus_isVisible
    - Running the test class shows FAILURE (all 3 RED) — output shows "Tests run: 3" with failures/errors; NOT a compilation error and NOT "Tests run: 0"
    - WsTestSupport-based tests (CallLifecycleTest, SingleSessionTest, etc.) still compile and are not broken
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test JVM → Spring ApplicationContexts | Two contexts share a single Redis container; test controls JWT minting via server-side JwtService — no client-supplied identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | CrossInstanceCallTest JWT minting | accept | Tokens minted server-side via JwtService.generateToken — test exercises the same auth path as production; no bypass |
| T-06-SC | Tampering | No new npm/pip/cargo installs | accept | No package manager installs in this plan; Testcontainers image names are pinned in TestcontainersConfiguration (redis:7-alpine, postgres:17-alpine, rabbitmq:4.1-management) |
</threat_model>

<verification>
After completing both tasks:

1. Compilation check: ./mvnw -pl backend compile exits 0
2. RED test check: ./mvnw -pl backend -Dtest=CrossInstanceCallTest test shows FAILURE (all 3 tests RED, not compile error)
3. Regression check: ./mvnw -pl backend -Dtest="CallLifecycleTest,SingleSessionTest,PresenceBroadcastTest,PresenceSweeperTest" test exits 0 (existing tests still pass with updated WsTestSupport/PresenceSweeper — they use Testcontainers Redis which still works)
</verification>

<success_criteria>
- PresenceSweeper.java has no LocalPresenceService reference and no PresenceWebSocketHandler field; constructor takes (StringRedisTemplate) only
- WsTestSupport.java field is "protected PresenceService presence" with no LocalPresenceService import
- CrossInstanceCallTest.java exists with 3 RED test methods
- ./mvnw compile exits 0
- Existing WsTestSupport-based test suite does NOT regress (callLifecycle/singleSession/presenceBroadcast/presenceSweeper tests still compile and pass)
</success_criteria>

<output>
Create .planning/phases/06-horizontal-scaling/06-01-SUMMARY.md when done
</output>
