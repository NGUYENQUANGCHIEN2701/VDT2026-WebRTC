---
phase: 07-group-mesh-calls
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/vdt/webrtc/room/RoomStateMachineTest.java
  - backend/src/test/java/com/vdt/webrtc/ws/RoomMeshTest.java
  - backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceRoomTest.java
  - frontend/src/webrtc/MeshManager.test.ts
  - frontend/src/webrtc/PeerManager.test.ts
autonomous: true
requirements:
  - ADV-03
must_haves:
  truths:
    - "Wave 0 RED tests exist for D-03 room cap enforcement, atomic concurrent joins at size 3, leave cleanup, and room key cleanup without orphaned room/user-room keys."
    - "RoomMeshTest exists and fails RED for D-04 joiner-initiates member-list flow, participant-left fanout, and server-side 5th-join rejection."
    - "CrossInstanceRoomTest exists and fails RED using the Phase 6 two-context harness so room fanout is proven cross-instance before implementation."
    - "MeshManager.test.ts exists and fails RED for D-06 dynamic RTCRtpSender.setParameters bitrate caps and uncapping when the room falls back to 2 participants."
    - "PeerManager.test.ts is extended so the new per-peer callback path is covered while the legacy 1-1 callStore fallback still passes unchanged (D-02 non-negotiable)."
    - "Existing 1-1 backend and frontend tests still compile after the new test scaffolding lands."
  artifacts:
    - path: "backend/src/test/java/com/vdt/webrtc/room/RoomStateMachineTest.java"
      provides: "RED Lua/repository tests for join_room.lua and leave_room.lua, including cap races and cleanup guarantees."
    - path: "backend/src/test/java/com/vdt/webrtc/ws/RoomMeshTest.java"
      provides: "RED WebSocket integration tests for create-room, join-room, participant-left, and room-full behavior."
    - path: "backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceRoomTest.java"
      provides: "RED two-instance integration scaffold proving room signaling must work across Redis-routed instances."
    - path: "frontend/src/webrtc/MeshManager.test.ts"
      provides: "RED frontend unit coverage for multi-peer lifecycle and bitrate cap transitions."
    - path: "frontend/src/webrtc/PeerManager.test.ts"
      provides: "Legacy-safe callback seam coverage for mesh without breaking 1-1."
  key_links:
    - from: "RoomStateMachineTest"
      to: "backend/src/main/resources/scripts/join_room.lua"
      via: "Lua atomic join contract from D-03"
      pattern: "SCARD.*SADD.*FULL"
    - from: "RoomMeshTest"
      to: "PresenceWebSocketHandler room dispatch"
      via: "create-room -> room-invite -> join-room -> room-joined/participant-joined/participant-left"
      pattern: "room-(invite|joined|full)|participant-(joined|left)"
    - from: "MeshManager.test.ts"
      to: "frontend/src/webrtc/PeerManager.ts"
      via: "setSendersMaxBitrate + per-peer callback seam"
      pattern: "setParameters|onConnectionStateChange"
---

## Phase Goal

**As a** planner setting up Phase 7 for disciplined execution, **I want** RED tests that lock the
room-cap, mesh-join, cross-instance, and bitrate-cap behaviors before implementation, **so that**
the additive group path can move quickly without risking the protected 1-1 core from D-02.

<objective>
Create the Wave 0 validation architecture required by 07-RESEARCH.md: `RoomStateMachineTest`,
`RoomMeshTest`, `CrossInstanceRoomTest`, `MeshManager.test.ts`, and targeted `PeerManager.test.ts`
updates. These tests MUST fail RED at first, but they must compile cleanly and encode the exact
Phase 7 contract: D-03 atomic server cap, D-04 joiner-initiates member lists and per-pair
politeness, D-05 partial-mesh leave/failure isolation, and D-06 dynamic bitrate caps.

This plan is additive only. It must not rewrite or weaken the 1-1 path, and it must prove that
any new frontend seam in `PeerManager` preserves the existing single-peer behavior untouched.
</objective>

<execution_context>
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/workflows/execute-plan.md
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/ROADMAP.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/REQUIREMENTS.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/07-group-mesh-calls/07-CONTEXT.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/07-group-mesh-calls/07-RESEARCH.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/07-group-mesh-calls/07-PATTERNS.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/07-group-mesh-calls/07-UI-SPEC.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/06-horizontal-scaling/06-CONTEXT.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write backend RED tests for room state, room mesh signaling, and cross-instance fanout</name>
  <files>
    backend/src/test/java/com/vdt/webrtc/room/RoomStateMachineTest.java,
    backend/src/test/java/com/vdt/webrtc/ws/RoomMeshTest.java,
    backend/src/test/java/com/vdt/webrtc/ws/CrossInstanceRoomTest.java
  </files>

  <action>
    Create three failing backend tests using the patterns already proven in Phases 4 and 6.

    `RoomStateMachineTest.java` must cover:
    - D-03 `join_room.lua`: join succeeds while room size is below 4, the 5th join returns FULL,
      and two simultaneous joins at size 3 produce exactly one winner.
    - `leave_room.lua`: removing the last participant deletes `room:{roomId}` and
      `user-room:{userId}` so Redis cleanup leaves no orphaned room keys.
    - Idempotent behavior for a repeated join by the same user and safe leave behavior.

    `RoomMeshTest.java` must cover:
    - D-01/D-04 create-room -> room-invite -> join-room -> room-joined member list flow.
    - Server-side room-full rejection with clear room-full message on the 5th join attempt.
    - Participant leave/drop broadcasts `participant-left` without breaking the remaining peers.
    - Existing 1-1 messages still route unchanged in the same WebSocket handler.

    `CrossInstanceRoomTest.java` must use the Phase 6 two-Spring-context harness and prove:
    - A joiner on instance B receives room fanout initiated on instance A.
    - Cross-instance participant-left fanout reaches remaining peers.
    - The room-full rule is enforced even when competing joins happen across instances.

    Keep the tests RED. They should fail because the room backend does not exist yet, not because
    the harness is malformed or the tests do not compile.
  </action>

  <verify>
    <automated>cd backend && ./mvnw -pl . -Dtest="RoomStateMachineTest,RoomMeshTest,CrossInstanceRoomTest" test</automated>
  </verify>

  <done>
    - All three backend test classes exist and compile.
    - The suite is RED for implementation reasons, not missing-class or syntax failures.
    - RoomStateMachineTest explicitly asserts no orphaned `room:*` / `user-room:*` state after the room empties.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Write frontend RED tests for the mesh seam, bitrate caps, and 1-1 compatibility</name>
  <files>
    frontend/src/webrtc/MeshManager.test.ts,
    frontend/src/webrtc/PeerManager.test.ts
  </files>

  <action>
    Add RED frontend tests that lock the Phase 7 browser-side seams before implementation.

    `MeshManager.test.ts` must cover:
    - One `PeerManager` per remote participant stored outside Zustand.
    - D-04 joiner-initiates add-peer flow and participant-left teardown of only that peer.
    - D-06 bitrate cap transitions: no cap at 2 total participants, cap applied at 3-4, cap removed
      again when the room shrinks back to 2.

    `PeerManager.test.ts` must be extended, not replaced:
    - Add coverage for the optional per-peer connection-state callback used by mesh.
    - Assert the legacy fallback still writes to the existing 1-1 `callStore` when no callback is
      provided, preserving D-02.
    - Add a focused test for `setSendersMaxBitrate()` updating only video senders via
      `RTCRtpSender.setParameters`.

    Keep the suite RED. The new tests should fail because the implementation is missing, while the
    existing PeerManager tests still protect the 1-1 path.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/webrtc/MeshManager.test.ts src/webrtc/PeerManager.test.ts</automated>
  </verify>

  <done>
    - MeshManager.test.ts exists and is RED for missing implementation.
    - PeerManager.test.ts includes new callback and bitrate cases while existing 1-1 assertions remain.
    - No change in this task requires altering `callStore` semantics.
  </done>
</task>

</tasks>

<threat_model>
security_enforcement: true
security_asvs_level: 1
blocking_threshold: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client -> backend room flow | Tests must preserve server-owned identity and must not encode any client-trusted `from` field assumptions. |
| Frontend mesh orchestration -> browser WebRTC APIs | Tests must keep `RTCPeerConnection`, sender parameters, and room state separated so non-serializable browser objects never leak into Zustand. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | room signaling tests | mitigate | Ensure tests assert server-generated sender identity and do not permit a client-crafted `from` to drive room events. |
| T-07-02 | Tampering | Redis cleanup assertions | mitigate | Explicit RED coverage for orphan cleanup on leave prevents silent key leaks that would corrupt future joins. |
| T-07-03 | DoS | room cap path | mitigate | Concurrent-join RED tests require the atomic Lua cap from D-03 before implementation can go GREEN. |
</threat_model>

<verification>
1. Backend RED gate: `./mvnw -pl . -Dtest="RoomStateMachineTest,RoomMeshTest,CrossInstanceRoomTest" test`
2. Frontend RED gate: `npx vitest run src/webrtc/MeshManager.test.ts src/webrtc/PeerManager.test.ts`
3. 1-1 compile gate: backend and frontend test commands still compile the existing 1-1 path without forced rewrites.
</verification>

<success_criteria>
- The required Wave 0 tests from 07-RESEARCH.md all exist.
- RED coverage explicitly includes D-03 cap races, D-04 joiner-initiates behavior, D-05 partial leave isolation, and D-06 bitrate caps.
- PeerManager test coverage proves the new mesh seam does not break 1-1.
- No deferred scope (SFU, recording, screen share, room links, host controls, 1-1 escalation) appears in the tests.
</success_criteria>

<output>
Create .planning/phases/07-group-mesh-calls/07-01-SUMMARY.md when done
</output>
