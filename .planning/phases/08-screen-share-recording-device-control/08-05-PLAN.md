---
phase: 08-screen-share-recording-device-control
plan: 05
type: verification
wave: 5
depends_on: [08-04]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/08-screen-share-recording-device-control/08-VALIDATION.md
autonomous: false
requirements:
  - ADV-01
  - ADV-02
  - MEDIA-03
  - MEDIA-04
must_haves:
  truths:
    - "All 4 Phase 8 success criteria from ROADMAP.md are manually verified: screen share with browser-bar stop, recording with composited preview, device switching, speaker selector."
    - "08-VALIDATION.md exists and documents the manual verification results."
    - "ROADMAP.md Phase 8 row is updated to reflect completion."
    - "Full test suite (frontend + backend) passes with no regressions."
    - "1-1 call smoke test still passes after Phase 8 additions."
  artifacts:
    - path: ".planning/phases/08-screen-share-recording-device-control/08-VALIDATION.md"
      provides: "Completed manual verification checklist for all Phase 8 behaviors."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete with date."
---

## Phase Goal

**As a** developer closing Phase 8, **I want** manual verification of all success criteria and a
clean test suite, **so that** Phase 8 is confirmed complete and Phase 9 planning can begin with
a clean slate.

<objective>
Execute the full Phase 8 manual verification checklist, run the complete automated test suite,
confirm all 4 success criteria are TRUE, document results in 08-VALIDATION.md, and update
ROADMAP.md. This is an autonomous-false wave — it requires two real browser sessions for
manual verification.
</objective>

<execution_context>
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/workflows/execute-plan.md
@D:/VDTWebRTC/VDT2026-WebRTC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/ROADMAP.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/REQUIREMENTS.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/08-screen-share-recording-device-control/08-CONTEXT.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/08-screen-share-recording-device-control/08-RESEARCH.md
@D:/VDTWebRTC/VDT2026-WebRTC/.planning/phases/08-screen-share-recording-device-control/08-UI-SPEC.md
</context>

<tasks>

<task type="automated">
  <name>Task 1: Run full automated test suite and build</name>
  <files>
    (no production changes — verification only)
  </files>

  <action>
    Execute all automated tests and build:

    1. `cd frontend && npx vitest run` — all tests must pass.
    2. `cd backend && ./mvnw -pl . test` — all backend tests must pass.
    3. `cd frontend && npm run build` — build must succeed.

    If any test fails, fix the regression before proceeding to manual verification.
    Document any non-trivial fix in the summary.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run && npm run build</automated>
    <automated>cd backend && ./mvnw -pl . test</automated>
  </verify>

  <done>
    - All Vitest tests pass.
    - All backend tests pass.
    - Frontend build succeeds.
  </done>
</task>

<task type="manual">
  <name>Task 2: 1-1 screen share manual verification</name>
  <files>
    (no production changes)
  </files>

  <action>
    Open two browser windows logged in as different users. Start a 1-1 call.
    Execute the following checks and record results in 08-VALIDATION.md:

    **SC-01: Screen share (ADV-01)**
    - [ ] Click "Share screen" → browser picker opens → select a window → remote sees screen content in their video surface.
    - [ ] Stop from app ("Stop sharing" button) → camera returns → remote sees camera again, no reconnect.
    - [ ] Start screen share again → stop from **browser toolbar X** → camera returns automatically → UI shows camera, not black/frozen.
    - [ ] Turn camera off while screen sharing → stop sharing → camera-off state is preserved (avatar/placeholder shows for remote), no camera re-enable.
    - [ ] Share button is disabled or titled correctly when getDisplayMedia is unavailable (simulate by overriding `navigator.mediaDevices.getDisplayMedia = undefined` in DevTools).
    - [ ] HUD pill "Sharing screen" appears while sharing; disappears on stop.
    - [ ] Blue MonitorUp icon on Share button reflects active state.

    **SC-02: Recording (ADV-02)**
    - [ ] Open More panel → click "Start recording" → recording HUD pill appears with timer.
    - [ ] Remote side sees "{username} is recording" indicator.
    - [ ] Click "Stop recording" → recording HUD disappears → "Recording ready" modal opens with playable preview.
    - [ ] Preview shows composited recording: remote video as main, local self-view as PiP overlay, audio mixed from both.
    - [ ] Click "Download recording" → `.webm` file downloads with correct filename `call-{callId}-{timestamp}.webm`.
    - [ ] Click "Close preview" → modal closes → remote recording indicator clears.
    - [ ] Hang up while recording → recording stops → remote indicator cleared → preview available if chunks exist.
    - [ ] "Start recording" is disabled if remote stream not yet connected (test by opening More immediately after call is initiated but before connection).

    Record any deviations from expected behavior with browser and version.
  </action>

  <done>
    - All SC-01 and SC-02 checks documented in 08-VALIDATION.md as PASS or FAIL with notes.
  </done>
</task>

<task type="manual">
  <name>Task 3: Device control manual verification (1-1 and group)</name>
  <files>
    (no production changes)
  </files>

  <action>
    Execute device control checks in a 1-1 call and a group call (3+ participants).
    Record results in 08-VALIDATION.md:

    **SC-03: Device switching (MEDIA-03)**
    - [ ] Open More panel in active 1-1 call → Camera selector shows available cameras.
    - [ ] Switch camera mid-call → remote video updates to new camera → mute/cam-off state preserved.
    - [ ] Switch microphone mid-call → remote hears new mic → mute state preserved (still muted if was muted).
    - [ ] Switch camera while screen sharing → only selectedCameraDeviceId updates → screen share continues uninterrupted → after stopping screen share, new camera is used.
    - [ ] Selecting an unavailable device → error toast shown → previous device still active → call continues.

    **SC-04: Audio output selection (MEDIA-04)**
    - [ ] If setSinkId is supported (Chrome/Edge): Speaker selector appears in More panel with available output devices.
    - [ ] Select a different speaker → audio output routes to selected device → remote audio audible from new speaker.
    - [ ] If setSinkId is NOT supported (Firefox/Safari): Speaker section is entirely absent (not disabled/hidden, truly absent).

    **Group call device checks:**
    - [ ] Start 3-person group call → open More panel → Camera selector works → all remote participants see new camera.
    - [ ] Switch microphone in group → all remote participants hear new mic.
    - [ ] Speaker selection applies to all remote participant tiles.
    - [ ] Screen share in group → all remote tiles update to screen content → browser toolbar stop restores camera for all.
    - [ ] New participant joining after screen share has started → receives screen share track correctly.
    - [ ] No "Start recording" control visible in group call More panel.

    Record browser, version, and outcome for each check.
  </action>

  <done>
    - All SC-03 and SC-04 checks documented as PASS or FAIL with notes.
    - Group call device checks documented.
  </done>
</task>

<task type="manual">
  <name>Task 4: 1-1 call smoke test (regression guard)</name>
  <files>
    (no production changes)
  </files>

  <action>
    After Phase 8 additions, confirm the core 1-1 call still works end-to-end:

    - [ ] User A calls User B → ringing shows on B's side.
    - [ ] B accepts → media connects → both see and hear each other.
    - [ ] Mute mic → remote sees mute indicator.
    - [ ] Toggle camera off → remote sees cam-off placeholder.
    - [ ] Hang up from A → both sides see call summary screen with correct end reason.
    - [ ] Call history shows the call for both users.
    - [ ] Admin dashboard shows call counted in daily stats.

    This is a regression smoke test — Phase 8 must not have broken the Phase 3/4/5 core.
  </action>

  <done>
    - 1-1 core smoke test documented in 08-VALIDATION.md as PASS.
    - Any regression identified and fixed (rare but possible after store field additions).
  </done>
</task>

<task type="documentation">
  <name>Task 5: Create 08-VALIDATION.md and update ROADMAP.md</name>
  <files>
    .planning/phases/08-screen-share-recording-device-control/08-VALIDATION.md,
    .planning/ROADMAP.md
  </files>

  <action>
    **Create `.planning/phases/08-screen-share-recording-device-control/08-VALIDATION.md`**
    using the results from Tasks 2-4. Structure:

    ```markdown
    # Phase 8 Validation

    **Date:** <date>
    **Browser(s) tested:** <Chrome xx, Edge xx, Firefox xx>

    ## Success Criteria Results

    | Criterion | Status | Notes |
    |-----------|--------|-------|
    | SC-01: Screen share with browser-bar stop auto-restores camera | PASS/FAIL | |
    | SC-02: Recording composited preview + remote indicator | PASS/FAIL | |
    | SC-03: Camera and microphone switch mid-call without disconnect | PASS/FAIL | |
    | SC-04: Speaker selector shows when supported, hidden when not | PASS/FAIL | |

    ## Manual Checklist

    ### 1-1 Screen Share
    (copy SC-01 items with PASS/FAIL)

    ### 1-1 Recording
    (copy SC-02 items)

    ### Device Controls — 1-1
    (copy SC-03/SC-04 items)

    ### Device Controls — Group Call
    (copy group items)

    ### 1-1 Core Smoke Test
    (copy smoke test items)

    ## Automated Test Results

    - Frontend Vitest: PASS (N tests)
    - Backend Maven: PASS (N tests)
    - Frontend Build: PASS

    ## Deviations / Known Gaps
    (list any FAIL items with plan to fix or accept)
    ```

    **Update `.planning/ROADMAP.md`**:
    - Change Phase 8 table row from `| 0/TBD | Not started |` to `| 5/5 | ✅ Complete | <date> |`.
    - Change `- [ ] **Phase 8:**` to `- [x] **Phase 8:**` in the phases list.
    - Update `*Phase 8 planned: ...` footer line to `*Phase 8 completed: <date>`.
    - Update `Phase 7` executing note if Phase 7 Wave 5 is also done by now.
  </action>

  <verify>
    <automated>(none — documentation task)</automated>
  </verify>

  <done>
    - 08-VALIDATION.md exists with all checklist items populated.
    - ROADMAP.md Phase 8 row shows ✅ Complete with date.
    - All 4 success criteria marked PASS (or remaining deviations documented and accepted).
  </done>
</task>

</tasks>

<threat_model>
security_enforcement: false
security_asvs_level: 1
blocking_threshold: low
</threat_model>

<verification>
Final automated gates:
1. `cd frontend && npx vitest run`
2. `cd backend && ./mvnw -pl . test`
3. `cd frontend && npm run build`

All 4 ROADMAP Phase 8 success criteria must be TRUE:
1. User can share screen → remote sees it → browser-bar stop restores camera automatically.
2. User can record 1-1 call and download composited .webm → remote sees recording indicator.
3. User can switch camera and microphone mid-call without dropping the connection.
4. Speaker selector appears on supported browsers; hidden on unsupported.
</verification>

<success_criteria>
- 08-VALIDATION.md documents all 4 success criteria as PASS.
- ROADMAP.md Phase 8 marked ✅ Complete.
- All automated tests pass.
- 1-1 core smoke test passes (Phase 3/4/5 not regressed).
- Phase 8 is ready to coexist with Phase 9 planning.
</success_criteria>

<output>
Create .planning/phases/08-screen-share-recording-device-control/08-05-SUMMARY.md when done
</output>
