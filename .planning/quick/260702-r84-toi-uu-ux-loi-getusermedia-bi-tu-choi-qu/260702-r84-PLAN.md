---
phase: quick-260702-r84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/webrtc/media.ts
  - frontend/src/webrtc/media.test.ts
  - frontend/src/realtime/callActions.ts
  - frontend/src/realtime/callActions.mediaError.test.ts
  - frontend/src/components/call/MediaErrorNotice.tsx
  - frontend/src/components/call/SelfViewPreview.tsx
  - frontend/src/components/call/CallLayer.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Khi startCall() gặp lỗi getUserMedia, SelfViewPreview KHÔNG tự reset về idle nữa — vẫn hiện MediaErrorNotice với nút 'Thử lại'; bấm nút này gọi lại acquireLocalMedia() mà không cần tải lại trang, thành công thì tiếp tục gửi call-invite như luồng bình thường."
    - "Copy lỗi 'permission-denied' không còn bảo 'tải lại trang' — thay bằng hướng dẫn ngắn gọn kèm chỉ dẫn bấm nút Thử lại."
    - "Khi callee bấm Nhận (acceptCall) mà bị từ chối quyền, IncomingCallCard KHÔNG biến mất ngay lập tức — toast báo lỗi hiện trước, call-reject bị trì hoãn ~1.75s để user kịp đọc rồi mới gửi, tránh caller đổ chuông vô thời hạn."
    - "Với lỗi no-device/overconstrained ở luồng caller, nút 'Tiếp tục với âm thanh' hiện trên MediaErrorNotice (trước đây bị ẩn vì SelfViewPreview không truyền onAudioOnly) và khi bấm sẽ chủ động xin lại CHỈ audio qua acquireAudioOnlyMedia(), thành công thì gửi call-invite ở chế độ audio-only."
    - "Mọi hành vi mới có test Vitest theo đúng style callActions.mediaError.test.ts hiện có; toàn bộ vitest + tsc -b + eslint vẫn xanh, không phá test cũ."
  artifacts:
    - "frontend/src/webrtc/media.ts — acquireAudioOnlyMedia() mới, copy 'permission-denied' cập nhật"
    - "frontend/src/realtime/callActions.ts — attemptOutgoingMedia() helper dùng chung, retryOutgoingMedia(), continueOutgoingAudioOnly(), acceptCall() trì hoãn reject, export teardownMedia() cho test isolation"
    - "frontend/src/components/call/MediaErrorNotice.tsx — prop onRetry mới + nút 'Thử lại'"
    - "frontend/src/components/call/SelfViewPreview.tsx — nhận onRetry/onAudioOnly, truyền xuống MediaErrorNotice"
    - "frontend/src/components/call/CallLayer.tsx — wire retryOutgoingMedia/continueOutgoingAudioOnly vào SelfViewPreview"
    - "frontend/src/webrtc/media.test.ts, frontend/src/realtime/callActions.mediaError.test.ts — test mới/updated cho toàn bộ hành vi trên"
  key_links:
    - "CallLayer → SelfViewPreview.onRetry/onAudioOnly → callActions.retryOutgoingMedia()/continueOutgoingAudioOnly() → attemptOutgoingMedia() → getMedia() → sendSignal('call-invite') — mắt xích nối nút bấm UI tới hành động thật; thiếu 1 khâu là nút bấm im lặng vô tác dụng."
    - "MediaErrorNotice chỉ render nút Thử lại khi onRetry được truyền, và nút audio-only khi c.fallback && onAudioOnly đều đúng — SelfViewPreview quên truyền 1 trong 2 prop sẽ làm nút biến mất âm thầm (đúng bug đang sửa cho onAudioOnly)."
    - "acceptCall() setTimeout(..., 1750) gửi rejectCall() phải guard bằng callId chụp lại tại thời điểm lỗi (so với useCallStore.getState().callId lúc timer bắn) — thiếu guard này, 1 cuộc gọi MỚI (callId khác, vd caller gọi lại ngay) có thể bị reject nhầm."
    - "Module-scoped localStream trong callActions.ts KHÔNG bị Zustand reset() dọn — test mới (retry/continue thành công) phải tự gọi teardownMedia() ở afterEach, nếu không getMedia() sẽ short-circuit (if (localStream) return true) và làm sai các test lỗi chạy SAU đó trong cùng file."
---

<objective>
Tối ưu UX cho 3 điểm cụ thể của luồng lỗi getUserMedia trong cuộc gọi 1-1, tiếp nối trực tiếp bug fix "cancel-call-permission-denied" (đã fix: call tự hủy đúng + toast báo lỗi khi permission bị từ chối, các file media.ts/callActions.ts/MediaErrorNotice.tsx hiện đang ở trạng thái ĐÃ FIX nhưng CHƯA COMMIT trong working tree):

1. Nút "Thử lại" trên MediaErrorNotice (caller/SelfViewPreview) — retry acquireLocalMedia() tại chỗ, không bắt tải lại trang; copy 'permission-denied' đổi từ "tải lại trang" sang hướng dẫn ngắn + trỏ nút Thử lại.
2. Callee (IncomingCallCard) có thời gian đọc toast lỗi trước khi card biến mất — trì hoãn rejectCall() ~1.75s sau khi acceptCall() bị từ chối quyền.
3. Wire onAudioOnly đã có sẵn trong MediaErrorNotice xuống SelfViewPreview + callActions, cho lỗi no-device/overconstrained ở luồng caller một đường "Tiếp tục với âm thanh" chủ động (dùng acquireAudioOnlyMedia() mới, không phải fallback tự động đã hỏng sẵn bên trong acquireLocalMedia()).

Purpose: Bug fix trước đã ngăn cuộc gọi bị treo khi mất quyền camera/mic, nhưng UX sau đó vẫn cụt — caller phải tải lại trang mới gọi lại được, callee gần như không kịp đọc lý do, và nút audio-only đã code sẵn nhưng chết vì thiếu 1 dòng wiring. Việc này khép lại 3 khoảng trống UX đó mà không đổi lại phần server-authoritative state machine đã đúng.

Output: MediaErrorNotice có nút Thử lại hoạt động thật; SelfViewPreview/CallLayer wire đủ 2 prop onRetry + onAudioOnly; callActions.ts có attemptOutgoingMedia()/retryOutgoingMedia()/continueOutgoingAudioOnly() dùng chung 1 đường xin media, acceptCall() trì hoãn reject có guard; media.ts có acquireAudioOnlyMedia() + copy mới; test Vitest phủ đủ 3 hành vi.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@frontend/src/webrtc/media.ts
@frontend/src/realtime/callActions.ts
@frontend/src/components/call/MediaErrorNotice.tsx
@frontend/src/components/call/SelfViewPreview.tsx
@frontend/src/components/call/CallLayer.tsx
@frontend/src/store/callStore.ts
@frontend/src/realtime/callActions.mediaError.test.ts
@.planning/debug/cancel-call-permission-denied.md
</context>

<constraints_critical>
**Working tree có nhiều thay đổi CHƯA COMMIT từ các phiên làm việc KHÁC** (kiểm tra `git status` trước khi bắt đầu): frontend/src/api/axios.ts, frontend/src/pages/CallPage.tsx, frontend/src/pages/GroupCallPage.tsx, frontend/src/realtime/roomActions.ts, frontend/src/webrtc/PeerManager.ts, frontend/src/webrtc/recording.ts, frontend/src/webrtc/recording.test.ts, frontend/src/realtime/roomActions.mediaError.test.ts, .planning/ROADMAP.md, .planning/debug/*.md, .planning/quick/260702-lva-*/, .planning/quick/260702-nqb-*/, .planning/phases/10-*/. TUYỆT ĐỐI KHÔNG revert/stash/checkout các file/thư mục này — build tiếp trên trạng thái working tree hiện tại, không đụng tới chúng.

Ba file frontend/src/webrtc/media.ts, frontend/src/realtime/callActions.ts, frontend/src/components/call/MediaErrorNotice.tsx CŨNG đang có thay đổi chưa commit — nhưng đó CHÍNH LÀ bug fix "cancel-call-permission-denied" mà plan này tiếp nối trực tiếp (đã đọc và xác nhận qua git diff). Sửa tiếp trên nội dung hiện tại của 3 file này là ĐÚNG Ý, không phải xung đột.

Khi commit: stage ĐÍCH DANH từng file bằng `git add <path>...` — KHÔNG dùng `git add -A` / `git add .`. Chỉ stage 7 file trong `files_modified` ở frontmatter (media.ts, media.test.ts, callActions.ts, callActions.mediaError.test.ts, MediaErrorNotice.tsx, SelfViewPreview.tsx, CallLayer.tsx). KHÔNG stage bất kỳ file nào khác kể cả khi chúng hiện đang modified.

Commit message: dùng conventional commit mô tả rõ nội dung thật (vd `feat(call): add retry and audio-only continue for getUserMedia errors`) — KHÔNG dùng format `docs(quick-...)` của GSD mặc định, KHÔNG thêm dòng `Co-Authored-By: Claude` (quy ước riêng của repo này, xem MEMORY.md).
</constraints_critical>

<tasks>

<task type="auto">
  <name>Task 1: Logic — retry/audio-only chủ động cho caller + trì hoãn reject cho callee</name>
  <files>frontend/src/webrtc/media.ts, frontend/src/realtime/callActions.ts</files>
  <action>
    **frontend/src/webrtc/media.ts:**
    1. Sửa `body` của entry `'permission-denied'` trong `MEDIA_ERROR_COPY`: bỏ hướng dẫn "tải lại trang", thay bằng câu ngắn hướng dẫn cấp lại quyền camera/mic trong trình duyệt rồi bấm nút "Thử lại" (khớp với nút mới sẽ thêm ở Task 2). Không đổi `heading`.
    2. Thêm hàm mới `export async function acquireAudioOnlyMedia(): Promise<LocalMedia>` ngay sau `acquireLocalMedia()`, tái dùng `AUDIO_CONSTRAINTS` đã có. Chỉ gọi `navigator.mediaDevices.getUserMedia({ video: false, audio: AUDIO_CONSTRAINTS })` — KHÔNG thử video, KHÔNG tự fallback lồng nhau (khác `acquireLocalMedia`, nơi fallback audio-only đã tự chạy và THẤT BẠI trước khi no-device/overconstrained được set — gọi lại y hệt logic cũ vô nghĩa; hàm này là đường retry audio-only RIÊNG, độc lập, có thể thành công nếu điều kiện mic đã đổi kể từ lần fail trước). Bắt lỗi và map theo `err.name`: `NotAllowedError`→`permission-denied`, `NotReadableError`→`device-busy`, `SecurityError`→`security-error`, `NotFoundError`→`no-device`, `OverconstrainedError`→`overconstrained`, còn lại→`unknown`. Thêm 1 đoạn comment tiếng Việt ngắn giải thích vì sao hàm này tách riêng khỏi `acquireLocalMedia` (retry audio-only chủ động của user, không phải fallback tự động).

    **frontend/src/realtime/callActions.ts:**
    1. Thêm `acquireAudioOnlyMedia` vào import từ `'../webrtc/media'` ở dòng import đầu file.
    2. Sửa `getMedia()` (hàm private hiện tại) thêm tham số tùy chọn `opts?: { audioOnly?: boolean }`; bên trong, nếu `opts?.audioOnly` thì gọi `acquireAudioOnlyMedia()`, ngược lại gọi `acquireLocalMedia()` như cũ. Giữ nguyên phần còn lại (short-circuit `if (localStream) return true`, `setMediaError(null)` đầu hàm, set `localStream`/`mediaMode` khi thành công, `setMediaError(type)` khi lỗi).
    3. Thêm hàm private mới `async function attemptOutgoingMedia(remoteUsername: string, opts?: { audioOnly?: boolean }): Promise<void>` đặt ngay trước `startCall`: gọi `getMedia(opts)`; nếu false thì `reportGetMediaError()` rồi return (KHÔNG reset UI, KHÔNG gửi tín hiệu gì — để callState ở nguyên 'outgoing' với mediaError hiện, cho user tự bấm Thử lại/Tiếp tục với âm thanh/Hủy); nếu true thì `sendSignal({ type: 'call-invite', to: remoteUsername })`.
    4. Sửa `startCall(remoteUsername: string)`: chỉ còn `useCallStore.getState().startOutgoing(remoteUsername, '')` rồi `await attemptOutgoingMedia(remoteUsername)`. XÓA phần try/catch cũ gọi `reportGetMediaError()` + `useCallStore.getState().reset()` trên nhánh lỗi (đổi hành vi CHỦ Ý theo yêu cầu điểm 1 của task này: không tự-reset-về-idle nữa, giữ overlay lại để user retry thay vì bị đá về Home). Cập nhật lại comment "Bugfix (cancel-call-permission-denied)" phía trên cho khớp hành vi mới (không còn reset, giờ giữ nguyên 'outgoing' để cho phép Thử lại/Tiếp tục âm thanh — Hủy vẫn hoạt động qua `cancelCall()` sẵn có vì `callId` vẫn rỗng).
    5. Thêm 2 hàm export mới ngay sau `startCall`:
       - `export async function retryOutgoingMedia(): Promise<void>` — đọc `{ callState, remoteUserId, callId }` từ store; guard `if (callState !== 'outgoing' || !remoteUserId || callId) return` (chỉ retry khi còn đang ở overlay outgoing, có remote, và call-invite CHƯA từng gửi thành công — callId thật chỉ được điền khi server trả 'ringing'); nếu qua guard thì `await attemptOutgoingMedia(remoteUserId)`.
       - `export async function continueOutgoingAudioOnly(): Promise<void>` — cùng guard hệt trên; gọi `await attemptOutgoingMedia(remoteUserId, { audioOnly: true })`.
    6. Sửa `acceptCall()`: thêm hằng số module-level `const CALLEE_REJECT_DELAY_MS = 1750` ngay phía trên hàm (comment ngắn: đủ để user đọc toast lỗi trước khi IncomingCallCard biến mất, theo khoảng 1.5-2s được đề xuất). Trong nhánh `if (!(await getMedia()))`: giữ `reportGetMediaError()` gọi ngay lập tức (toast hiện NGAY), nhưng thay lệnh gọi `rejectCall()` trực tiếp bằng: chụp lại `callId` hiện tại vào 1 biến local, rồi `setTimeout(() => { if (useCallStore.getState().callId === capturedCallId) rejectCall() }, CALLEE_REJECT_DELAY_MS)`. Guard theo callId là bắt buộc — nếu trong lúc chờ, cuộc gọi đã đổi/kết thúc vì lý do khác (callId đổi hoặc về null), KHÔNG được gửi reject nhầm. Cập nhật comment "Bugfix (cancel-call-permission-denied)" phía trên cho khớp: giờ trì hoãn reject thay vì gửi ngay.
    7. Đổi `function teardownMedia()` thành `export function teardownMedia()` (không đổi nội dung hàm) — cần export để test Task 3 tự dọn `localStream`/`peer` module-scope giữa các case (Zustand `reset()` không đụng tới state module-scope này).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <done>media.ts có acquireAudioOnlyMedia() export + copy permission-denied mới không còn nhắc "tải lại trang"; callActions.ts có attemptOutgoingMedia()/retryOutgoingMedia()/continueOutgoingAudioOnly() export, startCall() không còn tự reset khi lỗi, acceptCall() trì hoãn rejectCall() có guard theo callId, teardownMedia() đã export; tsc -b --noEmit sạch.</done>
</task>

<task type="auto">
  <name>Task 2: UI — wire nút Thử lại + Tiếp tục với âm thanh vào SelfViewPreview/CallLayer</name>
  <files>frontend/src/components/call/MediaErrorNotice.tsx, frontend/src/components/call/SelfViewPreview.tsx, frontend/src/components/call/CallLayer.tsx</files>
  <action>
    **frontend/src/components/call/MediaErrorNotice.tsx:**
    Thêm prop `onRetry?: () => void` vào type Props (cạnh `onAudioOnly?: () => void` hiện có). Bọc 2 nút (nếu có) trong 1 hàng flex chung, chỉ render hàng này khi `onRetry || (c.fallback && onAudioOnly)` để không tạo div rỗng khi cả hai đều absent: nút "Thử lại" render khi `onRetry` truyền (không phụ thuộc `c.fallback` — permission-denied/device-busy/security-error/unknown đều có thể retry, không riêng nhóm có fallback), style nổi bật hơn nút hiện có (nền `var(--accent)`, chữ trắng, bo tròn, giống ngôn ngữ `.popup-btn` đã dùng ở nơi khác trong app) vì đây là hành động sửa lỗi chính; nút "Tiếp tục với âm thanh" giữ nguyên điều kiện `c.fallback && onAudioOnly` và style text-link hiện có, đặt sau nút Thử lại trong cùng hàng.

    **frontend/src/components/call/SelfViewPreview.tsx:**
    Thêm 2 prop vào interface Props: `onRetry: () => void`, `onAudioOnly: () => void`. Truyền cả 2 xuống `<MediaErrorNotice type={mediaError} onRetry={onRetry} onAudioOnly={onAudioOnly} />` (nhánh `mediaError ? (...)` hiện tại chỉ truyền `type`).

    **frontend/src/components/call/CallLayer.tsx:**
    Import thêm `retryOutgoingMedia, continueOutgoingAudioOnly` từ `'../../realtime/callActions'` (cùng dòng import các hàm callActions hiện có). Truyền `onRetry={retryOutgoingMedia}` và `onAudioOnly={continueOutgoingAudioOnly}` vào `<SelfViewPreview>` đang render khi `callState === 'outgoing'`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/components/call/MediaErrorNotice.tsx src/components/call/SelfViewPreview.tsx src/components/call/CallLayer.tsx</automated>
  </verify>
  <done>SelfViewPreview truyền onRetry/onAudioOnly xuống MediaErrorNotice; MediaErrorNotice hiện nút Thử lại khi onRetry được truyền và nút Tiếp tục với âm thanh khi c.fallback && onAudioOnly; CallLayer wire cả 2 hàm từ callActions; tsc + eslint sạch.</done>
</task>

<task type="auto">
  <name>Task 3: Test Vitest cho 3 hành vi mới + full gate</name>
  <files>frontend/src/webrtc/media.test.ts, frontend/src/realtime/callActions.mediaError.test.ts</files>
  <action>
    **frontend/src/webrtc/media.test.ts:** thêm `acquireAudioOnlyMedia` vào import từ `./media` ở dòng import hiện có. Thêm describe block mới (theo đúng style `domError`/`getUserMedia` mock đã có trong file, KHÔNG đổi code hiện có): test (1) thành công → `mode` là `'audio-only'` và `getUserMedia` được gọi với `{ video: false, audio: expect.any(Object) }` (KHÔNG có video:true); (2) `NotAllowedError` → reject với `{ type: 'permission-denied' }`; (3) `NotFoundError` → reject với `{ type: 'no-device' }`.

    **frontend/src/realtime/callActions.mediaError.test.ts:**
    1. Thêm `acquireAudioOnlyMediaMock = vi.fn()` cạnh `acquireLocalMediaMock`, thêm `acquireAudioOnlyMedia: (...args: unknown[]) => acquireAudioOnlyMediaMock(...args)` vào object trả về của `vi.mock('../webrtc/media', ...)`. Reset mock này trong `beforeEach` (`acquireAudioOnlyMediaMock.mockReset()`).
    2. Import thêm `retryOutgoingMedia, continueOutgoingAudioOnly, teardownMedia` từ `./callActions` (cạnh `startCall, acceptCall` đã import). Thêm hằng `const fakeStream = { id: 's', getTracks: () => [] } as unknown as MediaStream` ở đầu file (mirror pattern trong media.test.ts).
    3. Đổi `afterEach(() => vi.unstubAllGlobals())` thành gọi thêm `teardownMedia()` sau `vi.unstubAllGlobals()` — bắt buộc để dọn `localStream` module-scope giữa các test (test mới có case thành công sẽ set `localStream`, nếu không dọn thì `getMedia()` ở test SAU sẽ short-circuit qua nhánh `if (localStream) return true` và bỏ qua `acquireLocalMediaMock`, làm sai kết quả).
    4. Sửa test hiện có trong describe `'startCall — getUserMedia bị NotAllowedError (permission-denied)'`: đổi kỳ vọng — KHÔNG còn `callState` về `'idle'` mà VẪN là `'outgoing'` (giữ overlay để user thấy nút Thử lại), `remoteUserId` vẫn là `'bob'`, `mediaError` là `'permission-denied'`; giữ nguyên 2 assertion `sendSignalMock` không gọi + toast xuất hiện đúng message. Đổi tên `it(...)` mô tả cho khớp hành vi mới.
    5. Sửa test `'gửi call-reject để báo caller...'` trong describe `acceptCall`: bọc bằng `vi.useFakeTimers()`/`try{...}finally{ vi.useRealTimers() }`; sau `await acceptCall()` assert: KHÔNG gọi `call-accept`, toast đã hiện (1 phần tử, message đúng), và `sendSignalMock` CHƯA được gọi lần nào (vì reject đang bị trì hoãn); sau đó `await vi.advanceTimersByTimeAsync(1750)` rồi assert `sendSignalMock` đã được gọi với `{ type: 'call-reject', callId: 'call-123' }`. Đổi tên `it(...)` phản ánh việc trì hoãn.
    6. Thêm describe mới `'retryOutgoingMedia — user bấm "Thử lại"...'` với 3 test: (a) retry thành công (`acquireLocalMediaMock.mockResolvedValueOnce({ stream: fakeStream, mode: 'video' })` sau khi `startCall` đã fail trước đó) → `mediaError` về null, `sendSignalMock` được gọi với `{ type: 'call-invite', to: 'bob' }`; (b) retry vẫn lỗi → không gửi call-invite, `mediaError` vẫn `'permission-denied'`, toast mới xuất hiện; (c) gọi `retryOutgoingMedia()` khi store đang ở `idle` (sau `reset()`) → no-op, `acquireLocalMediaMock` không được gọi.
    7. Thêm describe mới `'continueOutgoingAudioOnly — user bấm "Tiếp tục với âm thanh"...'` với 1 test: sau khi `startCall('bob')` fail với `MediaAcquisitionError('no-device')`, gọi `continueOutgoingAudioOnly()` với `acquireAudioOnlyMediaMock.mockResolvedValueOnce({ stream: fakeStream, mode: 'audio-only' })` → assert `acquireAudioOnlyMediaMock` được gọi đúng 1 lần (KHÔNG phải `acquireLocalMediaMock`), `useCallStore.getState().mediaMode` là `'audio-only'`, `mediaError` về null, `sendSignalMock` gọi với `{ type: 'call-invite', to: 'bob' }`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/webrtc/media.test.ts src/realtime/callActions.mediaError.test.ts && npx vitest run && npx tsc -b --noEmit && npx eslint .</automated>
  </verify>
  <done>Toàn bộ test mới xanh; full vitest suite xanh (không phá test cũ); tsc -b --noEmit sạch; eslint . sạch trên toàn repo frontend.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` — toàn bộ suite xanh, bao gồm test mới cho acquireAudioOnlyMedia, retryOutgoingMedia, continueOutgoingAudioOnly, và acceptCall trì hoãn reject.
- `cd frontend && npx tsc -b --noEmit` — sạch.
- `cd frontend && npx eslint .` — sạch.
- Smoke thủ công (khuyến nghị, không bắt buộc để hoàn tất task): chặn quyền camera trong browser → bấm Gọi → thấy MediaErrorNotice + nút Thử lại; cấp lại quyền → bấm Thử lại → cuộc gọi tiếp tục bình thường. 2 tab: callee chặn quyền rồi bấm Nhận → thấy toast lỗi VÀ IncomingCallCard còn hiện thêm ~1.75s trước khi biến mất.
- `git status` sau commit: các file KHÔNG thuộc `files_modified` (axios.ts, CallPage.tsx, GroupCallPage.tsx, roomActions.ts, PeerManager.ts, recording.ts/test.ts, roomActions.mediaError.test.ts, ROADMAP.md, debug/*.md, quick/260702-lva-*/, quick/260702-nqb-*/, phases/10-*/) vẫn ở nguyên trạng thái modified/untracked trước đó — không bị stage nhầm.
</verification>

<success_criteria>
- Caller có đường thoát khỏi lỗi getUserMedia mà không cần F5: nút Thử lại gọi lại acquireLocalMedia() tại chỗ.
- Copy permission-denied không còn bảo user tải lại trang.
- Callee có đủ thời gian đọc lý do lỗi trước khi IncomingCallCard biến mất (trì hoãn reject ~1.75s, có guard an toàn theo callId).
- Nút "Tiếp tục với âm thanh" hoạt động thật cho no-device/overconstrained ở luồng caller, dùng đường xin audio riêng (acquireAudioOnlyMedia) thay vì lặp lại fallback tự động đã hỏng.
- Không đổi hành vi server-authoritative call state machine đã đúng từ trước; chỉ mở rộng UX xử lý lỗi phía client.
- Commit chỉ chứa đúng 7 file của plan này; các thay đổi chưa commit khác trong working tree còn nguyên vẹn.
</success_criteria>

<output>
Tạo `.planning/quick/260702-r84-toi-uu-ux-loi-getusermedia-bi-tu-choi-qu/260702-r84-SUMMARY.md` khi hoàn tất.
</output>
