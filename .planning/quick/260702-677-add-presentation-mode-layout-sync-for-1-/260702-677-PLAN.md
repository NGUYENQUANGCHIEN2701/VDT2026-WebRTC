---
phase: quick-260702-677
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/callStore.ts
  - frontend/src/store/callStore.test.ts
  - frontend/src/realtime/callActions.ts
  - frontend/src/pages/CallPage.tsx
  - frontend/src/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Khi bên kia bấm Share screen trong cuộc gọi 1-1, phía người xem tự chuyển sang layout trình chiếu: màn hình được chia sẻ hiển thị lớn (object-fit contain, không bị crop), bên còn lại thu nhỏ thành tile góc"
    - "Khi mình bấm Share screen, chính mình cũng thấy layout trình chiếu (màn hình mình chia sẻ lớn, remote thu nhỏ) — đối xứng 2 phía"
    - "HUD pill phía người xem hiển thị '{remoteUserId} is sharing' (giống tiền lệ GroupCallPage); phía người chia sẻ vẫn thấy 'Sharing screen'"
    - "Recording 1-1 khi REMOTE đang chia sẻ sẽ vẽ video của remote vào vùng presentation-main (không còn chỉ nhận biết local share)"
    - "Tắt share (bất kỳ phía nào) → layout trở về bình thường (remote full + self PiP); reset() store xóa remoteIsScreenSharing"
  artifacts:
    - "frontend/src/store/callStore.ts — field remoteIsScreenSharing + setter, clear trong reset()"
    - "frontend/src/store/callStore.test.ts — test Vitest mới cho remoteIsScreenSharing"
    - "frontend/src/realtime/callActions.ts — handler media-state-relay áp dụng msg.isScreenSharing"
    - "frontend/src/pages/CallPage.tsx — activeSharer derivation + presentation layout + HUD pill + getActiveSharer recording"
    - "frontend/src/index.css — CSS classes presentation cho 1-1"
  key_links:
    - "callActions.ts handleServerSignal 'media-state-relay' → useCallStore.setRemoteIsScreenSharing(msg.isScreenSharing) — mắt xích đang đứt: viewer không bao giờ biết peer đang share"
    - "CallPage startRecording getActiveSharer trả về ĐÚNG chuỗi remoteLabel đã truyền vào RecordingController (remoteUserId ?? 'Remote') — selectSharerVideo match theo label, sai chuỗi là compositor vẽ placeholder"
    - "Hai thẻ <video> (remoteRef/selfRef) KHÔNG được unmount khi đổi layout — srcObject gán qua effect chỉ chạy theo callState/streamVersion, remount là đen màn hình"
---

<objective>
Đồng bộ presentation-mode layout cho màn gọi 1-1 khi có screen share (CallPage.tsx), theo đúng pattern activeSharer mà GroupCallPage.tsx đã giải quyết cho group call.

Purpose: Hiện tại phía người XEM trong cuộc gọi 1-1 hoàn toàn không biết peer đang chia sẻ màn hình — wire format `media-state`/`media-state-relay` đã mang sẵn `isScreenSharing` (messages.ts dòng 25, 54) và `sendCurrentMediaState()` đã GỬI nó, nhưng handler nhận (callActions.ts, case `'media-state-relay'`) bỏ rơi field này với một comment đã lỗi thời. Video màn hình chia sẻ bị render như camera thường (object-fit cover → crop mất nội dung), không có layout focus, HUD không báo, recording không biết remote đang share.

Output: Store field mới + wire sync + presentation layout 2 người trên CallPage + HUD pill người xem + recording getActiveSharer đúng cho cả 2 chiều + Vitest cho store.

SCOPE LOCKED — KHÔNG mở rộng: không làm camera+screen đồng thời (dual video track / renegotiation) — đã defer theo quyết định của user. Giữ nguyên cơ chế single-video-track replaceTrack hiện có.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/CallPage.tsx
@frontend/src/pages/GroupCallPage.tsx
@frontend/src/pages/GroupCallStyles.css
@frontend/src/store/callStore.ts
@frontend/src/realtime/callActions.ts
@frontend/src/realtime/messages.ts
@frontend/src/webrtc/recording.ts
@frontend/src/index.css
</context>

<constraints_critical>
**Working tree có thay đổi CHƯA COMMIT từ phiên làm việc khác** trong: CallPage.tsx, GroupCallPage.tsx, recording.ts, recording.test.ts, GroupCallModal.tsx, docker-compose.yml. TUYỆT ĐỐI KHÔNG revert / stash / checkout các file này — build tiếp trên trạng thái working tree hiện tại.

Khi commit: stage ĐÍCH DANH từng file của task này (callStore.ts, callStore.test.ts, callActions.ts, CallPage.tsx, index.css) bằng `git add <path>...` — KHÔNG dùng `git add -A` / `git add .`. CallPage.tsx đang có hunk chưa commit từ việc khác nhưng cùng feature area → commit cả file là chấp nhận được. KHÔNG stage: GroupCallPage.tsx, GroupCallModal.tsx, recording.ts, recording.test.ts, docker-compose.yml, .planning/config.json, .planning/debug/.

Commit message: conventional commit mô tả rõ (vd `feat(call): sync presentation-mode layout for 1-1 screen sharing`) — KHÔNG dùng format `docs(quick-...)`, KHÔNG thêm Co-Authored-By Claude (quy ước repo này).
</constraints_critical>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Track remoteIsScreenSharing trong callStore và áp dụng từ media-state-relay</name>
  <files>frontend/src/store/callStore.ts, frontend/src/store/callStore.test.ts, frontend/src/realtime/callActions.ts</files>
  <behavior>
    - Test 1: store mặc định remoteIsScreenSharing === false
    - Test 2: setRemoteIsScreenSharing(true) → getState().remoteIsScreenSharing === true; set lại false → false
    - Test 3: set true rồi gọi reset() → remoteIsScreenSharing về false (và các field khác về default như hiện tại)
  </behavior>
  <action>
    1. Tạo file test mới `frontend/src/store/callStore.test.ts` (Vitest, cùng convention colocated như src/realtime/wsClient.test.ts): import useCallStore, thao tác qua useCallStore.getState() / setState. Vì store là module singleton, mỗi test gọi useCallStore.getState().reset() trong beforeEach để cách ly. Viết 3 test theo behavior trên. Chạy — phải ĐỎ (field chưa tồn tại / TS lỗi).
    2. `frontend/src/store/callStore.ts`: thêm vào interface CallStoreState field `remoteIsScreenSharing: boolean` và `setRemoteIsScreenSharing: (v: boolean) => void`, đặt cạnh cụm remoteMicMuted/remoteCamOff (dòng ~31-34) cho nhất quán. Trong creator: default `false`, setter theo pattern các setter hiện có, và THÊM `remoteIsScreenSharing: false` vào object trong `reset()` (dòng ~126 — object reset liệt kê tường minh từng field, quên là test 3 fail).
    3. `frontend/src/realtime/callActions.ts`, trong `handleServerSignal`, case `'media-state-relay'` (dòng ~391-396): thêm `call.setRemoteIsScreenSharing(msg.isScreenSharing)` cạnh setRemoteMicMuted/setRemoteCamOff. XÓA khối comment lỗi thời ngay sau case đó (dòng ~397-400, khối bắt đầu "Note: 1-1 calls do not use msg.isScreenSharing...") — nội dung của nó nay sai; thay bằng 1 dòng comment ngắn đúng thực tế (isScreenSharing của peer drive presentation layout trên CallPage).
    4. Chạy lại test — XANH.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/store/callStore.test.ts</automated>
  </verify>
  <done>callStore.test.ts xanh (3 test); callStore có field + setter + reset clear; media-state-relay handler gọi setRemoteIsScreenSharing; comment lỗi thời đã thay.</done>
</task>

<task type="auto">
  <name>Task 2: Presentation layout 1-1 trên CallPage + HUD pill người xem + recording getActiveSharer 2 chiều</name>
  <files>frontend/src/pages/CallPage.tsx, frontend/src/index.css</files>
  <action>
    **Nguyên tắc bất di bất dịch:** hai thẻ `<video>` (remoteRef, selfRef) phải LUÔN mounted ở nguyên vị trí trong JSX tree — chỉ đổi className/style theo trạng thái. KHÔNG viết 2 nhánh JSX riêng (presentation vs normal) kiểu GroupCallPage, vì CallPage gán srcObject qua effect phụ thuộc [callState, remoteStreamVersion] / [localStreamVersion]; unmount-remount là mất srcObject → đen màn hình. (GroupCallPage không dính vấn đề này vì ParticipantTile tự gán stream nội bộ.)

    1. **Derive activeSharer** trong CallPage: subscribe thêm `remoteIsScreenSharing` từ useCallStore; khai báo `const activeSharer: 'local' | 'remote' | null = isScreenSharing ? 'local' : remoteIsScreenSharing ? 'remote' : null` (local thắng nếu cả hai cùng share — 1-1 không có server-side single-sharer lock).

    2. **CSS mới trong index.css**, đặt ngay sau khối `.self-video`/`.call-video--dimmed` (dòng ~1442-1459), mượn ngôn ngữ thị giác của GroupCallStyles.css (.presentation-main: border 2px solid #3b82f6, border-radius 16px, background #000) nhưng là class riêng cho 1-1 vì không có sidebar/thumbnail strip:
       - `.call-video--presenting`: video đang chia sẻ chiếm stage, `object-fit: contain` (BẮT BUỘC — cover sẽ crop nội dung màn hình), background #000, viền accent xanh #3b82f6, border-radius 16px; co stage lại theo inset kiểu presentation (tham chiếu `inset: 80px 24px 140px` của .presentation-layout để không đè HUD trên và bottom bar).
       - `.call-video--pip`: tile nhỏ góc dưới-phải cho bên KHÔNG chia sẻ, thông số theo `.self-video` hiện có (width min(240px, 28vw), aspect-ratio 16/10, border-radius 10px, z-index 15, object-fit cover, cùng vị trí right 20px / bottom 104px).
       - `.self-video--presenting`: self video chiếm stage khi mình share — override position absolute inset theo presentation, width/height 100%, object-fit contain, background #000, viền xanh, z-index thấp hơn PiP.

    3. **Gán class động trong JSX** (giữ nguyên cấu trúc section.call-video-stage):
       - Video remote: className hiện tại (`call-video` + `--dimmed` khi reconnecting) cộng thêm: activeSharer === 'remote' → `call-video--presenting`; activeSharer === 'local' → `call-video--pip`.
       - Video self: khi activeSharer === 'local' thêm `self-video--presenting`; transform đổi thành không mirror khi đang share (`isScreenSharing ? 'none' : 'scaleX(-1)'` — mirror scaleX(-1) sẽ lật ngược chữ trên màn hình chia sẻ); các trạng thái khác giữ nguyên (class `self-video`, visibility theo camOff — lưu ý khi local share thì camOff đã bị ép false bởi startScreenShare nên không xung đột).
       - `RemoteCamOffOverlay` và `RemoteMuteIndicator` là overlay kích thước stage: chỉ render khi activeSharer !== 'local' (khi remote bị thu thành PiP, overlay full-stage sẽ che màn hình mình đang share).
    
    4. **HUD pill** (dòng ~212-217): mở rộng điều kiện thành `(isScreenSharing || remoteIsScreenSharing)`; nội dung: đang tự share → giữ 'Sharing screen'; chỉ remote share → `` `${remoteUserId} is sharing` `` (đúng tiền lệ GroupCallPage dòng 255).

    5. **startRecording** (dòng ~146-158): khai báo `const remoteLabel = remoteUserId ?? "Remote"` một lần, dùng cho CẢ prop `remoteLabel` LẪN `getActiveSharer` — hai chuỗi phải trùng nhau vì selectSharerVideo trong recording.ts match remote theo label. getActiveSharer mới đọc store tại thời điểm gọi: isScreenSharing → 'local'; remoteIsScreenSharing → remoteLabel; còn lại → null. Xóa comment lỗi thời dòng ~150 ("Remote isScreenSharing isn't tracked...").

    KHÔNG sửa recording.ts / recording.test.ts (đang có thay đổi chưa commit từ việc khác; API getActiveSharer/selectSharerVideo đã hỗ trợ sẵn remote label — chỉ cần caller truyền đúng).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx vitest run</automated>
  </verify>
  <done>Build TS sạch, toàn bộ Vitest xanh. CallPage: activeSharer === 'remote' → remote video lớn object-fit contain + self giữ PiP; activeSharer === 'local' → self video lớn không mirror + remote thành PiP; activeSharer === null → layout cũ nguyên vẹn; HUD hiển thị "{remoteUserId} is sharing" phía viewer; getActiveSharer trả 'local' / remoteLabel / null đúng 3 trạng thái; hai thẻ video không bao giờ unmount khi đổi layout.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` — toàn bộ suite xanh (bao gồm callStore.test.ts mới và recording.test.ts sẵn có).
- `cd frontend && npm run build` — TypeScript + Vite build sạch.
- Smoke thủ công (nếu chạy được 2 tab): A share screen → B thấy màn hình A lớn (không crop) + pill "A is sharing"; A thấy màn hình mình lớn + B thu nhỏ; A tắt share → cả 2 về layout thường; B ghi hình khi A share → file webm vẽ màn hình A vào vùng main.
- `git status` sau commit: GroupCallPage.tsx, GroupCallModal.tsx, recording.ts, recording.test.ts, docker-compose.yml, .planning/config.json, .planning/debug/ vẫn ở trạng thái modified/untracked (không bị stage nhầm).
</verification>

<success_criteria>
- Viewer 1-1 nhận biết peer đang share qua wire field isScreenSharing sẵn có (mắt xích media-state-relay được nối).
- Presentation layout đối xứng 2 phía, màn hình chia sẻ hiển thị object-fit contain, không unmount video elements.
- Recording 1-1 vẽ đúng sharer (local hoặc remote) vào vùng presentation.
- Không đụng dual-track camera+screen (out of scope, đã defer).
- Commit chỉ chứa 5 file của task; các thay đổi chưa commit từ việc khác còn nguyên.
</success_criteria>

<output>
Tạo `.planning/quick/260702-677-add-presentation-mode-layout-sync-for-1-/260702-677-SUMMARY.md` khi hoàn tất.
</output>
