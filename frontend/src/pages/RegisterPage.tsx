export default function RegisterPage() {
  // TODO (bạn code): form username/email/password → gọi POST /api/auth/register
  //  - hiển thị fieldErrors khi 400; xử lý 409 (trùng user)
  return <div>Register Page — TODO</div>
}
